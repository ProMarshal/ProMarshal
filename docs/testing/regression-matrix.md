# Regression Matrix

Track historical bugs and permanent regression coverage.

| ID | Area | Prior Failure | Guardrail/Test | Status |
|---|---|---|---|---|
| REG-001 | PM Board -> Pulse deep link | Filter param leaked across nav and caused flicker/redirect loops | Navigation query scoping and roundtrip tests | Active |
| REG-002 | PM Board/Pulse health consistency | Card counts diverged from filtered list | Shared criteria contract + cross-view validation | Active |
| REG-003 | Team Poll routing | `skip` (without active poll) routed to Cortex task search | Terminal poll routing guard + negative intent tests | Active |
| REG-004 | Cortex read responses | Responses contradicted tool result scope/count | Authoritative tool-result grounding checks | Active |
| REG-005 | Session bootstrap/index drift | Session index conflict caused Cortex bootstrap failure | Safe index ensure logic + migration-safe checks | Active |
| REG-006 | Cortex self-scope parity | Equivalent self-scope prompts (`list my tasks` vs `how many tasks i have`) produced divergent counts | ID-first assignee resolution and self-scope parity tests across prompt variants | Active |
| REG-007 | Team Poll routing isolation | `skip` without active poll routed into Cortex task search | Terminal poll-route precedence + negative-path tests | Active |
| REG-008 | Team Poll owner ack consistency | Skip button and skip free text returned different acknowledgement texts | Unified `owner_skip_success` action/response contract test | Active |
| REG-009 | PM navigation URL hygiene | `healthStatus` leaked across nav causing PM/Pulse loop/flicker | URL scoping cleanup + nav roundtrip tests across tabs/navs | Active |
| REG-010 | Recommendation incremental recompute coverage | Jira task updates in webhook/brain-sync paths invalidated PM cache but could miss recommendation dirty markers, delaying incremental recompute | Dirty-marker wiring in webhook + brain-sync paths with focused unit coverage (`test_brain_sync_recommendation_dirty.py`) | Active |
| REG-011 | Cadence mutation recommendation lag | Cadence task updates (status/comment/backlog pick) could rely on delayed webhook-only propagation, leaving recommendation runtime stale between runs | Dirty-marker wiring in cadence mutation handler with focused unit coverage (`test_cadence_recommendation_dirty.py`) | Active |
| REG-012 | Completed-task recommendation residue | Completed tasks could remain in active recommendation runtime payload when no fresh dirty events existed | Incremental no-dirty cleanup path removes completed recommendations (`test_recommendation_orchestrator.py`) | Active |
| REG-013 | Commitment ETA miss not escalating | Past-due commitments could remain `open` until a full recompute, delaying escalation in PM alerts/actions | Incremental no-dirty commitment re-evaluation transitions `open -> missed` and escalates recommendation severity/action (`test_recommendation_orchestrator.py`) | Active |
| REG-014 | Extractor schema drift degrades recommendation text quality | LLM output shape changes could break text-signal parsing and silently degrade recommendation quality | Strict schema validation + deterministic fallback in recommendation text extractor (`test_recommendation_text_extractor.py`) | Active |
| REG-015 | Gateway runtime unavailable/retry routing regressions | Shared gateway failures could bypass fallback envelope or skip model fallback/retry behavior, destabilizing recommendation extraction | Concrete LiteLLM gateway tests for unavailable envelope + retry/fallback model attempt behavior (`test_litellm_gateway.py`) | Active |
| REG-016 | Sprint-end risk capability drift | Sprint-end escalation (`R8`) could trigger in projects where sprint capability is explicitly disabled, or fail to escalate when sprint capability is active with qualifying evidence | Capability-aware `R8` guard (`has_sprints` metadata) plus deterministic sprint-state/end checks with unit coverage in recommendation engine tests (`test_recommendation_engine.py`) | Active |
| REG-017 | Owner recommendation instability | Unassigned-risk recommendations could choose overloaded owners or unstable candidates without explicit load metrics | Owner load-metric scoring and ranking tests (`test_owner_recommender.py`) with confidence-tiered action handling in engine tests (`test_recommendation_engine.py`) | Active |
| REG-018 | Threshold hardcoding drift | Hardcoded rule thresholds could prevent per-project/workspace tuning and force code changes for policy adjustments | Threshold resolver + per-project override wiring with dedicated tests (`test_recommendation_thresholds.py`) and threshold-aware rule tests (`test_recommendation_engine.py`) | Active |
| REG-019 | Recommendation telemetry loss | Recommendation read/feedback interactions could be dropped or untracked, preventing adoption metrics and tuning loops | Telemetry persistence is intentionally disabled (no-op hook) to avoid unbounded event growth in Mongo; recommendation APIs remain functional without telemetry dependency (`test_recommendation_telemetry.py`, `test_project_recommendations_endpoint.py`) | Inactive (Disabled by Policy) |
| REG-020 | Recommendations endpoint recompute thrash | Repeated `/recommendations` reads could trigger full health-to-recommendation recompute every request, increasing latency/load and causing avoidable hot-path pressure | Short TTL read-through cache (`60-120s`) + cache hit/miss endpoint tests + invalidation coverage on orchestrator writes (`test_recommendation_runtime_cache.py`, `test_project_recommendations_endpoint.py`, `test_recommendation_orchestrator.py`) | Active |
| REG-021 | R6 overload blind spot without explicit task load score | Near-due overloaded tasks could miss overload guidance when `assignee_load_score` was absent on the task, especially when auxiliary throughput/timezone signals were unavailable or intentionally disabled | Capability-aware R6 fallback in recommendation engine: use owner metrics when available, else degrade to baseline workload-only scoring (`open_wip + overdue + due_48h`) with dedicated coverage (`test_recommendation_engine.py`) | Active |
| REG-022 | R9 false critical escalation on weak stale evidence | Critical-priority tasks could be incorrectly escalated as stale/critical on weak text-only stale hints when review capability is explicitly disabled, or when `updated_at` evidence is missing | R9 stale-evidence guardrails in recommendation engine: missing-timestamp protection + capability-aware text-stale gating (`has_reviews=false` suppresses text-only escalation) with regression coverage (`test_recommendation_engine.py`) | Active |
| REG-023 | Owner recommendation confidence drift | Owner suggestion confidence could be unstable/overconfident when load-gap-only scoring ignored throughput signal quality | Throughput-aware owner confidence and new recommender tests (`test_owner_recommender.py`) validating high-confidence and close-candidate confidence behavior | Active |
| REG-024 | Owner tie-break mismatch across timezones | Similar-load owner candidates could be ranked without considering collaboration-hour overlap, causing poor handoff velocity | Optional timezone-overlap scoring in owner metrics/ranking/confidence with tie-break coverage (`test_owner_recommender.py`) | Active |
| REG-025 | Project timezone propagation drift | Recommendation evaluation could run without project timezone context if callers omitted timezone on health payload composition, making timezone-aware scoring inconsistent across routes/jobs | Router + orchestrator project-timezone propagation tests (`test_project_recommendations_endpoint.py`, `test_pm_board_summary_recommendations_contract.py`, `test_recommendation_orchestrator.py`) | Active |
| REG-026 | Cortex mutation incremental trigger lag | Cortex Jira write tools could miss immediate recommendation dirty-mark signaling when BrainSync fallback/webhook compensation path was active, delaying incremental recompute after status/comment updates | Direct dirty-marker emission in Cortex Jira mutation tools + focused regression coverage (`test_cortex_jira_mutation_recommendation_dirty.py`) | Active |
| REG-027 | Commitment ETA missing or weakly sourced in runtime state | Recommendation runtime records could persist `commitment_eta=None` (or ignore explicit owner commitment signals / compact-offset timestamps), delaying `open -> missed` lifecycle transitions between full recomputes | ETA sourcing precedence in orchestrator runtime payload (explicit commitment fields -> due-date fields -> ISO ETA text extraction with compact timezone offset normalization) + full/incremental recompute coverage (`test_recommendation_orchestrator.py`) | Active |
| REG-028 | Project-type threshold policy drift | Thresholds could remain global-only when project type changes, causing mismatched recommendation sensitivity across implementation vs software workflows | Project-type-aware threshold resolution (defaults -> project-type profile -> project override) with settings-backed profile map and resolver tests (`test_recommendation_thresholds.py`) | Active |
| REG-029 | Cadence SharedAgent mutation dirty-trigger gap | Cadence SharedAgent tool wrappers could persist status/comment changes without direct recommendation dirty-mark hedging, delaying incremental recompute when downstream sync confirmation was deferred | Direct dirty-marker hedging in Cadence SharedAgent update/comment tools + focused tests (`test_cadence_agent_tools_recommendation_dirty.py`) | Active |
| REG-030 | TaskService sync-failure trigger gap | Shared task write service could commit Jira create/mutate but skip immediate recommendation dirty-mark when Brain sync failed, relying only on delayed webhook compensation | TaskService fallback dirty-mark on Brain sync failure for create/mutate paths + focused tests (`test_task_service_recommendation_dirty_fallback.py`) | Active |
| REG-031 | Dirty-pass completion residue | Incremental recompute could leave completed-task recommendations active when provider completion semantics were not strict `status in {done,closed,...}` (for example `is_closed=true` with non-final status labels), causing stale active recommendations | Provider-agnostic completion pruning in orchestrator merge/cleanup paths (`is_closed` + normalized status/status_category/resolution checks) with regression coverage (`test_recommendation_orchestrator.py`) | Active |
| REG-032 | Provider-path dirty-trigger drift in Cadence adapters | New/non-Jira Cadence provider adapters could miss recommendation dirty-mark wiring if capability IDs differed from Jira-specific names, delaying incremental recompute after status/comment updates | Centralized dirty-mark hedging in cadence adapter registry dispatch with capability-aware matching (`task_*` and `workitem_*` mutators) + registry dispatch tests (`test_cadence_action_adapter_registry.py`) | Active |
| REG-033 | Workspace policy threshold drift | Recommendation thresholds could ignore workspace-level operating norms, leading to inconsistent risk sensitivity across workspaces with different cadence/SLAs | Workspace-aware threshold resolution (settings + project doc map) with precedence tests in threshold resolver (`test_recommendation_thresholds.py`) | Active |
| REG-034 | Threshold update cache staleness | Admin threshold changes could persist in project config but remain invisible until cache TTL expiry, causing recommendation output to lag behind configured policy | Threshold admin update path invalidates supporting recommendations cache and PM summary cache, with endpoint coverage for read/write/clear behavior (`test_project_recommendations_endpoint.py`) | Active |
| REG-035 | Cadence adapter nested task-key extraction gap | Non-Jira cadence adapters returning nested task envelopes (for example `result.task.id`) could skip recommendation dirty-marking, delaying incremental recompute after mutation events | Cadence adapter registry task-key resolver now checks generic aliases and nested `result.task` identifiers, with dispatch regression test (`test_cadence_action_adapter_registry.py`) | Active |
| REG-036 | Commitment lifecycle stuck at `open` after completion | Incremental no-dirty commitment re-evaluation could transition `open -> missed` on ETA overrun but never `open -> met` when the task was completed, leaving runtime lifecycle state stale | Commitment lifecycle re-evaluation now reads current task completion state and transitions `open -> met` with regression coverage (`test_recommendation_orchestrator.py`) | Active |
| REG-037 | Jira import dirty-trigger omission | One Jira import sync path could upsert imported tasks without adding dirty markers, so if post-import full generation failed, incremental recompute could miss imported tasks until a later full run | Shared Jira import upsert helper now marks dirty per imported task key across import paths, with focused coverage (`test_integrations_jira_import_recommendation_dirty.py`) | Active |
| REG-038 | Commitment status initialization drift on completed tasks | Runtime recommendation records could initialize `commitment_status=open` even when task completion evidence was already present, causing noisy/later lifecycle corrections | Runtime payload now initializes commitment status from completion semantics (`met` for completed tasks) with regression coverage (`test_recommendation_orchestrator.py`) | Active |
| REG-039 | Full recompute completion residue | Scheduled/post-import full generation could reintroduce completed-task recommendations if completion pruning ran only in incremental paths | Full generation now applies completion-prune before runtime writes, with orchestration coverage (`test_recommendation_orchestrator.py`) | Active |
| REG-040 | Import blocked by dirty-marker failure | Jira import upsert could fail hard when dirty-marker emission raised (for example Redis transient failure), preventing task sync continuity | Jira import dirty-marking is now best-effort/non-blocking with regression coverage (`test_integrations_jira_import_recommendation_dirty.py`) | Active |
| REG-041 | Cadence nested payload identifier miss | Provider adapters that carry task/workitem identifiers only in nested request envelopes (`payload.task.id`, `payload.workitem.id`) could skip recommendation dirty-marking, delaying incremental recompute | Cadence registry task-key resolver now inspects nested payload envelopes in addition to flat/result fields, with dispatch regression coverage (`test_cadence_action_adapter_registry.py`) | Active |
| REG-042 | Webhook dirty-trigger key/failure drift | Jira webhook recompute triggering could be skipped when `issue.key` was absent (despite normalized key availability), or webhook processing could be disrupted by dirty-marker backend failures | Webhook task-key resolution now falls back to normalized `external_id` and dirty-mark writes are best-effort/non-blocking, with helper-level regression coverage (`test_jira_webhooks_recommendation_dirty.py`) | Active |
| REG-043 | PM Health hierarchy tip clipping | Tip popovers in the `Tip` column could be clipped by the card container boundary, making recommendation text partially hidden near lower rows | Health hierarchy detail card now allows overflow-visible so tip popovers can render outside row bounds; validated with manual hover checks on nested rows in Project Health | Active |
| REG-044 | Recommendation card duplicate reason text | Recommendation cards could show the same text twice when backend emitted `why` containing the same message as `reason` | Card renderer now normalizes and de-duplicates `why` entries against `reason` (case-insensitive), and removes duplicate `why` bullets before rendering | Active |
| REG-045 | Not-started recommendation noise | `todo/backlog/open` items could appear in recommendations without concrete risk qualifiers, creating noisy repetitive cards | Recommendation engine now gates not-started items to explicit risk qualifiers (due soon/overdue, missed start, high-priority unassigned, explicit rule signals, or critical severity) with coverage in `test_recommendation_engine.py` | Active |
| REG-046 | Status-name hardcoding drift across providers/workflows | Recommendation gating based on hardcoded raw status names could misclassify custom provider/project statuses and produce noisy or missing recommendations | Engine now uses normalized `status_category` (`not_started\|in_progress\|done\|blocked\|unknown`) with optional per-project `status_category_map` overrides wired through PM summary/recommendations composition, covered in normalized model + engine tests | Active |
| REG-047 | PM/Pulse recommendation duplication and noisy PM Board cards | PM Board showed detailed repeated recommendation items while Pulse also exposed task-level risk context, causing duplicated scan effort and clutter on large task sets | PM Board now renders high-level recommendation summary with CTA into Pulse Work Item detail; Pulse Work Item renders grouped recommendation actions with filter-aware task links and preserves legacy `tab=Project Health` route compatibility via aliasing | Active |
| REG-048 | Cortex follow-up write target drift | Follow-up mutation prompts (for example "move both to done") could execute against unrelated targets when prior result-set references were not deterministically grounded | Structured session snapshot (`target_refs`) + shared executor non-idempotent write grounding gate (message/snapshot/fresh-read) with fail-closed clarification on unresolved scope | Active |
| REG-049 | Due-date intent count/list drift | Equivalent prompts for "without due date" could return inconsistent totals/lists because due-date filtering was inferred differently across analyze/search paths | Canonical `due_date_mode` (`any\|missing\|present`) in shared task-read intent + shared filter contract + provider query translation with focused parser/repository/tool tests | Active |
| REG-050 | Recommendation status-category drift for custom workflows | Custom/new Jira statuses could be classified as `unknown` or mis-gated (especially start-date hygiene), causing noisy or missing recommendations | Project-level `recommendation_status_category_map` bootstrap/refresh on Jira connect+sync with pending unknown tracking, plus start-date best-practice gating to started categories only (`status_category != not_started/done`) with recommendation engine tests | Active |
| REG-051 | Incremental recompute base loss on Redis-cold runtime | Incremental merge could run against empty project payload when Redis project recommendation key expired, causing temporary partial recommendation lists | Redis-first + Mongo snapshot fallback load path with bootstrap full fallback for empty-base dirty recompute, plus snapshot write-through on full/incremental payload persistence | Active |
| REG-052 | Recommendation scheduler seeding provider hardcoding | Projects with connected workitem providers other than Jira category contract could miss recommendation schedule seeding due to provider-name filters | Scheduler eligibility now checks connected integrations by category (`workitem`) with Jira backward compatibility and preserves project-timezone full/incremental schedule creation | Active |
| REG-053 | PM Board cache-miss request hang/retry storm | PM Board summary cache miss could keep frontend in long refresh loop (request abort/retry churn) and delay/lose visible backend progress under slow compute paths | Async cache-miss contract: `/pm-board-summary` returns `202 processing` + enqueues worker recompute, `/pm-board-summary-status` polling drives client re-fetch on ready, with sync fallback if queue unavailable | Active |
| REG-054 | Projects route refetch churn and cold-paint delay | PM Board nav/tab URL sync used App Router `router.replace`, causing repeated server route re-execution (`/api/projects`) and slower first paint after reload; PM summary in-memory cache also reset on reload so UI waited for network despite recent client data | Switch internal nav/tab URL sync to `window.history.replaceState` (no server navigation) and hydrate PM summary from browser session cache (TTL-bound) before network revalidation in `projects-page.tsx` | Active |
| REG-055 | PM summary worker job import failure (`Invalid attribute name`) | PM summary async jobs could be enqueued with non-importable function names (for example bare `process_pm_board_summary_job`) and remain stuck in queued/failed loops | Enqueue PM summary job with callable reference (`process_pm_board_summary_job`) instead of string path literal to force stable import path serialization by RQ | Active |
| REG-056 | Cortex write clarification token leak | Write validation/quality failures could surface internal field tokens (for example `jira_add_comment.comment`) instead of user-friendly clarification labels | Centralized clarification formatter (`render_missing_field_labels` / `build_missing_fields_response`) used across orchestrator + executor paths with focused unit coverage (`test_cortex_clarification_and_tool_guard.py`, `test_cortex_executor_quality_policy.py`) | Active |
| REG-057 | Cortex write repair loop/latency amplification | Multiple write-quality/proposal-repair attempts in one turn could increase latency and churn without improving resolution | Shared per-turn repair budget (`cortex_max_repair_attempts_per_turn`) enforced in proposal and quality repair paths with deterministic fail-fast-to-clarification behavior | Active |
| REG-058 | Proposal quality false-block on explicit user fields | Semantically explicit fields extracted from user text could still be over-validated and blocked when confidence metadata existed but was unused | Proposal `field_meta` provenance+confidence wiring into `quality_prevalidated_fields` so explicit/deterministic/follow-up fields can bypass unnecessary semantic re-checks; covered by proposal metadata tests (`test_cortex_clarification_and_tool_guard.py`) | Active |
| REG-059 | Recommendation over-emission from implicit hygiene defaults | Recommendation engine could emit extra best-practice cards when hygiene fields were absent (not explicitly missing), causing noisy counts and rule-collision with risk recommendations | Best-practice rules now trigger only on explicit empty hygiene fields (`assignee_name/assignee`, `due_date`, `start_date`), preserving deterministic risk recommendation expectations (`test_recommendation_engine.py`) | Active |
| REG-060 | Recommendation orchestration DB snapshot hard-failure in no-DB test/runtime paths | Incremental/full recommendation orchestration could raise when Brain collection or snapshot persistence was unavailable (`brain_db None`), failing otherwise valid no-dirty/cleanup transitions | Added safe Brain collection access + fail-safe snapshot persistence in recommendation orchestrator; no-dirty paths continue deterministically without hard crash (`test_recommendation_orchestrator.py`) | Active |
| REG-061 | DM routing precedence policy token drift | Session isolation guard test expected legacy global-scope policy token presence to ensure DM routing precedence check remained stable | Restored backward-compatible `promarshal_global_scope_policy_enabled` token and guarded scope refusal branch to keep routing-order contract explicit (`test_session_isolation.py`) | Active |
| REG-062 | PM board snapshot write/read crash when Brain DB unavailable | PM board summary route could hard-fail with HTTP 500 when snapshot read/write attempted while Brain DB handle was unavailable (`brain_db None`) | PM board snapshot helpers now fail-open with guarded collection access and best-effort upsert/read behavior, preserving summary response contract (`test_pm_board_summary_recommendations_contract.py`) | Active |
| REG-063 | Jira mention token not linked to actual user identity | Comment text could include plain `@name` token without Jira account mention entity, resulting in visual text without real Jira user mention | Jira adapter now resolves comment mention handles to Jira account IDs and emits ADF `mention` nodes with safe ambiguity fallback to plain text; coverage added in `test_jira_comment_mentions.py` | Active |
| REG-064 | Jira comment confirmation text drift | Slack confirmation could show a rephrased comment while Jira stored raw command-style text, causing user-visible mismatch in committed action wording | Shared Jira comment mutation paths now deterministically compose command-style text into final comment text; `posted_comment_text` is carried through mutation/tool result so confirmation can reflect committed Jira payload. Deterministic planner extraction also covers `update <task> comments, <text>` phrasing to avoid empty-comment quality blocks. Coverage in `test_comment_composer.py`, `test_cortex_jira_mutation_recommendation_dirty.py`, and `test_cortex_deterministic_comment_extraction.py` | Active |
| REG-065 | Cadence/Team Poll parity drift during shared runtime hardening | Shared runtime hardening in Cortex could unintentionally change Cadence/Team Poll terminal routing or owner-skip acknowledgment semantics | Phase 4 parity pack asserts Team Poll owner free-text skip keeps `owner_skip_success` contract and router terminal-order boundaries remain intact for Team Poll and Cadence (`test_phase4_cadence_team_poll_parity_pack.py`) | Active |
| REG-066 | Follow-up Jira comment phrasing extraction gap | Follow-up-style wording such as `task PRM-59. comment: ...` was not captured by deterministic planner extraction, causing missed `comment` slot and avoidable clarification | Jira deterministic comment pattern now supports `task/issue <id> comment(s): <text>` form, with regression coverage in `test_cortex_deterministic_comment_extraction.py` | Active |
| REG-067 | Pending clarification follow-up lost write target grounding | After missing-field clarification (for example missing comment), follow-up value-only replies could be blocked by write target grounding because target refs were not carried from pending tool call context | Pending-question follow-up now injects target refs from prepared calls into runtime/session grounding context, and distinct new intents can supersede pending clarification cleanly (`test_cortex_pending_question_followup.py`) | Active |
| REG-068 | Cadence modal reopen blocked after close-without-submit | Cadence modal open button clicks were deduped like mutation actions, so closing a modal and clicking again within dedupe TTL could silently fail to reopen | Slack interactions router now bypasses dedupe for `open_update_modal` while retaining dedupe for mutation-like actions, with focused coverage (`test_slack_cadence_interactions_router.py`) | Active |
| REG-069 | Cadence Jira comment attribution showed session owner id | Cadence write payloads could use `session.user_id` (internal/project-owner-ish id) as `updated_by`, leading Jira comments to show incorrect actor attribution | Cadence mutation payloads now resolve actor identity from session member email/name with fallback; Jira cadence adapter applies same fallback, with helper-level coverage (`test_cadence_jira_task_adapters.py`) | Active |
| REG-070 | Cadence daily summary missing after timeout due to index drift | Timeout finalization and summary gating depended on `session_index`; cleanup/index drift could leave source sessions present but invisible to expiry/summary pipeline | Terminal-only cadence index cleanup, dedicated `cadence_expiry` scheduler tick, and source-session fallback for expiry/summary gating; coverage in `test_cadence_summary_terminal_gate.py`, `test_cadence_session_expiry_clarification.py`, `test_session_cleanup_cadence_index_policy.py` | Active |
| REG-071 | PM Board action-item summary drift from Pulse list | Action-item signal could diverge if PM Board read path did not compose open-count through `/pm-board-summary`, causing stale/mismatched board count vs Pulse tab | PM summary now composes `action_items.open_count` from project Brain and invalidates PM cache on action-item create/update/close; UI deep-link routes from PM Board to Pulse Action Items using the composed payload | Active |
| REG-072 | Action-item closure visibility ambiguity in Pulse | Closed and cancelled action items could look identical to open notes, making completion state easy to miss in member and PM review | Pulse Action Items sticky-note cards now apply status-specific styling (`done` strike-through, `cancelled` red strike-through), show `closed_at`, and support quick close actions with follow-up refresh | Active |
| REG-073 | Action-item digest quick-action dead-end | Digest reminders sent plain text only, so users could not complete quick close flows directly from Slack and interaction routing had no action-item branch | Scheduler digest now emits Slack block actions (`mark done`, `cancel`, `open full list`) and Slack interaction router dispatches namespaced action-item action IDs to dedicated handler with membership validation and service-backed close execution (`test_action_items_digest_interactions.py`) | Active |
| REG-074 | Action-item ingestion dedupe policy drift | Auto-detect dedupe could become effectively permanent due unique key constraints, diverging from 7-day policy and preventing valid re-ingest after dedupe expiry | Introduced project-scoped ingest ledger with TTL-backed 7-day dedupe window, duplicate-safe replay return path, and action-item index refactor to `_ensure_named_index` with migration off legacy unique ingest-key index (`test_action_items_service.py`) | Active |
| REG-075 | Action-item create failed on index ensure with Mongo code 67 | Shared named-index helper always passed `expireAfterSeconds=None`; Mongo rejected non-TTL index creation (`TTL index 'expireAfterSeconds' option must be numeric`) and blocked action-item create flow | `_ensure_named_index` now omits `expireAfterSeconds` unless explicitly set, plus helper regression tests for non-TTL vs TTL behavior (`test_database_index_helpers.py`) | Active |
| REG-076 | Action-item create failed on sequence counter upsert with Mongo code 40 | Counter allocator used conflicting update operators on the same field (`$inc.value` and `$setOnInsert.value`) causing `ConflictingUpdateOperators` in live Mongo writes | Sequence allocator now initializes counter via `$inc` only (no `$setOnInsert.value`) with regression coverage asserting update payload shape (`test_sequence.py`) | Active |
| REG-077 | Pulse action-item note overlap and reorder flicker on status updates | Sticky notes could overlap and reorder by `updated_at`, causing 2nd/3rd cards to jump/flicker on update and making multi-note boards unstable | Pulse Action Items now use fixed-gap note layout and stable `display_key (ACTION-#)` sorting; card actions are local state patches (no full-list refresh), with added `cancel` action and in-card color picker update path | Active |
| REG-078 | Pulse action-item filter complexity and admin scannability drift | Date dropdown-based filtering did not match action-item review workflow and long per-member lists were hard to scan in admin view | Pulse Action Items now use explicit view modes (`Default`, `Open`, `Closed`, `Archive`) with client-side status/age filtering, compact note sizing, `ACTION-` display-key label mapping, and per-member scroll regions in admin view | Active |
| REG-079 | Action-item document null-field bloat and sticky-note context clutter | Newly created/updated action items persisted many nullable fields (`closed_*`, `source_event_key`, `needs_followup_set_at`, optional context fields) and Pulse notes showed context labels from `related_type`, reducing clarity | Action-item service now compacts create payloads and uses `$unset` on updates for cleared optional fields so DB docs stay minimal; Pulse sticky-note card no longer renders `related_type` context line | Active |
| REG-080 | Counter key naming ambiguity for shared sequence utility | Action-item counter used a feature-specific legacy key (`action_item_display_key`) that looked non-reusable and conflicted with agreed namespaced convention for shared counter docs | Action-item counter now uses canonical `counter_key=sequence:action_item:display_key` with one-time create-path migration from legacy keys (`action_item_display_key`, `act_display_key`) to preserve sequence continuity | Active |
| REG-081 | Cortex action-item owner alias (`me`) failed membership validation | Natural-language owner hints (for example `assign to me`) could pass unresolved `owner_user_id=\"me\"` into ActionItemService and fail with `owner_user_id must be an active project member` despite valid actor membership | Shared identity resolver now supports actor-self aliases via optional actor context; Action Item Cortex tools resolve owner hints through shared resolver before create/list/update and return consistent follow-up messaging for ambiguous/not-found owners | Active |
| REG-082 | Action-item auto-extraction dropped important time/date details | Queue extraction used raw truncation (`text[:180]`) so temporal intent (for example `tomorrow 10am`) could be omitted in generated title and due hints | Action-item extraction now uses gateway-backed candidate resolver with deterministic preprocessing, confidence+retry fallback, and time-hint preservation guardrails (`test_action_item_title_extractor.py`, `test_action_items_extraction_job.py`) | Active |
| REG-083 | Action-item due date remained missing/misaligned when event time was present | Action item creation could capture event intent text (for example meeting tomorrow 11am) but persist `no_due_date`, missing logical pre-event follow-up deadlines | Action-item create paths now apply deterministic temporal grounding (`target_datetime`) and due inference policy (explicit due wins; otherwise infer pre-event actionable due) with service-level regression coverage (`test_action_items_service.py`) | Active |
| REG-084 | Cortex mixed action-item outcomes produced contradictory response text | In a single turn, action-item create success plus assignment failure could produce free-form contradictory wording ("created and assigned" + "couldn't assign") | Orchestrator now logs structured per-run tool outcomes and normalizes partial action-item write outcomes to deterministic `Completed`/`Not completed` text from grounded tool metadata (`test_cortex_action_item_partial_response.py`) | Active |
| REG-085 | Weekday-only temporal phrases and stale model due-date override | Inputs like "for Tuesday at 10am" could be mis-grounded (or overridden by hallucinated historical due date from model args), producing incorrect persisted due dates (for example 2023) | Temporal parser now supports plain weekday tokens; service precedence ignores model-provided due values when user text indicates target context (`for/on ...`) and no explicit due markers (`by/due/before`), with focused parser/service tests (`test_action_item_temporal_parser.py`, `test_action_items_service.py`) | Active |
| REG-086 | Noisy owner hint sentence failed self-assignment resolution | Cortex action-item create could receive full utterance as `owner_user_id` (instead of `me`) and return owner resolution failure even when the user clearly asked `assign to me` | Shared identity resolver now recognizes self-assignment phrases inside noisy hints (`assign to me`, `for me`, `to me`, `myself`) when actor context is available; covered by resolver and tool regressions (`test_identity_resolver.py`, `test_cortex_action_item_tools.py`) | Active |
| REG-087 | Pending clarification intercepted new create intent messages | When an action-item clarification was pending, a new DM intent like `create action item ...` could be misrouted as clarification reply, bypassing Cortex handoff and producing owner-resolution follow-up noise | Pending handler now short-circuits on new-intent patterns (`create/add/new/list/show/view/close/cancel/done/update` + `action item/task`) and returns `handled=False` so the message flows to Cortex; covered by `test_action_item_pending_handler.py` | Active |
| REG-088 | Create-success followed by non-blocking update-target clarification produced misleading partial-failure text | In some create-intent turns, model invoked `action_item_update` after successful `action_item_create`; write-target grounding correctly blocked update, but user saw confusing `Not completed: which items to update...` despite successful creation | Orchestrator partial-outcome normalizer now suppresses `action_item_update` `target_scope_clarification_required` failures when create committed action exists, returning deterministic create-success text; covered by `test_cortex_action_item_partial_response.py` | Active |
| REG-089 | Action-item create missed owner/time details from DM intent text | Cortex create could succeed with missing owner and no due inference when model omitted args; tool raw text resolver missed `latest_user_message`, and owner self-intent in full utterance was not deterministically backfilled | Action item create tool now prioritizes `latest_user_message`, backfills owner via shared identity resolver on full utterance when owner arg is absent, and emits explicit follow-up summary when `needs_followup=true`; covered by `test_cortex_action_item_tools.py` | Active |
| REG-090 | Action-item create response/title drift from persisted payload | LLM could paraphrase create confirmations (dropping temporal context in title and rendering relative due text) even when persisted fields were correct, causing user-visible mismatch | Action item create now applies temporal title-coverage backfill from raw user text and emits deterministic create summary from persisted payload; orchestrator prefers committed deterministic create summary for `action_item_create` turns (`test_cortex_action_item_tools.py`, `test_cortex_action_item_partial_response.py`) | Active |
| REG-091 | Late-night invite due-date clamp produced incorrect `00:xx` due times | Prior due inference policy could clamp inferred due to `now+30m` when invite event was next day, producing incorrect due values like `00:28` instead of requested event time | Due inference now uses deterministic v1 rule: when target datetime is present and no explicit due keywords are provided, persist `due_date = target_datetime`; action-item title temporal tokens are normalized to explicit absolute datetime text in project timezone (`test_action_items_service.py`, `test_cortex_action_item_tools.py`) | Active |
| REG-092 | Action-item create response leaked internal target-time inference context | Create confirmations could expose target-datetime normalization artifacts in user-facing output, even though target time is intended for due calculation only | Cortex action-item tool responses now keep `target_datetime` internal-only (removed from serialized response payload) and stop title temporal-token rewriting to absolute timestamps; user-facing create summary shows only title/assignee/due fields (`test_cortex_action_item_tools.py`, `test_cortex_action_item_partial_response.py`) | Active |
| REG-093 | Implicit owner auto-assignment when owner not explicitly stated | Cortex create could auto-assign owner from model/context even when user did not specify assignment intent in message | `action_item_create` now applies strict explicit-owner-intent gating (`assign to`/`owner`/self-assignment phrases). Without explicit owner intent, owner is forced null and follow-up is required; covered by `test_cortex_action_item_tools.py` | Active |
| REG-094 | Pending clarification could remain active and intercept DMs beyond escalation window | Action-item clarification pending records were created without `expires_at` and pending-read helpers did not clean/filter expired rows, allowing stale interactions to keep intercepting new messages | Action-item pending creation now sets `expires_at` aligned to configured escalation window (4h), and pending interaction read helpers delete expired records before lookup while filtering only unexpired pending rows (`test_action_items_service.py`, `test_pending_interactions.py`) | Active |
| REG-095 | Reopen action item cleared existing due date in UI/API roundtrip | PATCH update route used `model_dump()` and sent unset optional fields as `None`; reopen payload unintentionally included `due_date=None`, causing backend update to unset persisted due date | Action-item PATCH route now uses `model_dump(exclude_unset=True)` so only explicitly provided fields are updated; added router regression test asserting reopen payload does not include due fields (`test_action_items_router.py`) | Active |
| REG-096 | Action-item extraction worker skipped create path due to uninitialized DB handle | `action_item_extraction` RQ job executed outside API process bootstrap and called `ActionItemService.create` with `get_database()==None`, causing `'NoneType' object has no attribute 'projects'` and silent non-creation despite `Job OK` | Action-item job entrypoint now ensures Mongo connection initialization (`connect_to_mongo`) before extraction processing; debug guardrails added for enqueue/start/skip/create/fail branches (`api/app/action_items/jobs.py`, `api/app/action_items/extraction.py`) | Active |
| REG-097 | Action-item extraction worker event-loop mismatch (`Event loop is closed`) | Task-comment extraction invokes multiple `_run_async(...)` stages per job (candidate extract, then create). Mongo client created in stage-1 loop was reused in stage-2 loop after stage-1 loop closed, causing Motor executor calls against a closed loop | Extraction runner now applies per-run mongo readiness check (`ping`/reconnect) inside each `_run_async` execution loop, and reconnects on stale-loop handles before awaiting stage coroutines (`api/app/action_items/extraction.py`, `api/app/action_items/jobs.py`) | Active |
| REG-098 | Task-comment auto extraction forced actor assignment and lost explicit owner intent | Comment-derived action items were always created with `owner_user_id=actor_user_id`, so owner phrases (for example `ask sridevi to test again`, `sridevi to test again`) incorrectly assigned to actor and skipped clarification | Task-comment extraction now defaults to unassigned unless owner resolves explicitly; owner intent parsing expanded (`ask X to`, `X to <verb>`, `have/get/let X to`, `X should`, `assign ... X`) and shared identity resolution drives assignment/follow-up; auto title includes task-key context suffix `(TASK-KEY)` (`api/app/action_items/extraction.py`) | Active |
| REG-099 | Pending action-item clarification blocked unrelated DM conversations | When a pending clarification existed, non-assignment user messages were intercepted and responded to as clarification prompts, preventing normal Cortex handling | Pending handler now intercepts only assignment-intent replies (`assign/owner/set owner/give it to/...`) and passes unrelated/new-intent messages through (`handled=False`) so conversation flow remains unblocked (`api/app/action_items/pending_handler.py`) | Active |
| REG-100 | Dramatiq worker crash on ElastiCache Serverless/Redis cluster Lua dispatch | Dramatiq RedisBroker default namespace generated keys across different hash slots; Redis cluster rejected Lua dispatch with `Script attempted to access keys that do not hash to the same slot` and workers restarted continuously | Dramatiq broker now uses hash-tagged namespace `"{dramatiq}"` so Lua script keys map to one slot in cluster mode (`api/app/workers/dramatiq_app.py`); deployment also keeps `dramatiq_lua_max_stack` env override for Serverless compatibility | Active |
| REG-100 | Legacy cron overlap caused duplicate scheduler trigger noise during dispatcher cutover | Running both old cron endpoints and new dispatcher endpoint in migration can double-trigger scheduler ticks and obscure rollout observability | Added `POST /api/cron/tick` dispatcher lane orchestration + `SCHEDULER_USE_DISPATCHER` legacy endpoint skip contract (`{"skipped": true, "reason": "dispatcher_mode", "endpoint": ...}`) with coverage in `test_cron_dispatcher.py` and rollout plan in `docs/execution/scheduler-dispatcher-consolidation-plan-2026-03-16.md` | Active |
| REG-101 | Dispatcher tick overload from overlap + per-minute seed churn | 1-minute cron triggers could overlap and stack long-running dispatcher work; schedule seeding on every tick adds avoidable DB load and latency jitter | Added Redis-backed dispatcher tick lock (`tick_lock_active` skip), seed throttling (`scheduler_dispatcher_seed_interval_minutes`), and per-job limit overrides (`scheduler_job_type_limits_json`) with regression coverage in `test_cron_dispatcher.py` | Active |
| REG-102 | Cortex early-ack suppression for repeated DM/thread requests | Slack response dedupe keys for `ack` in DM/thread paths were keyed by channel/thread + text only, so distinct new requests with similar text inside dedupe TTL could suppress early ack unexpectedly | Response gate keys now include `event_ts` token even for DM/thread anchors so new events receive ack while Slack retry duplicates remain deduped; covered by `test_slack_router_response_guards.py` (`test_dm_ack_gate_key_is_event_scoped`, `test_thread_ack_gate_key_is_event_scoped`) | Active |
| REG-103 | Pending clarification routing drift across Team Poll and Action Items | Pending DM handling logic was feature-local, making precedence/failure behavior inconsistent and increasing risk of one feature intercepting messages meant for another | Introduced shared typed pending-interaction handler framework with centralized precedence (`team_poll` > `action_item`) and explicit fail-closed/fail-open policy, plus precedence and fallback regressions in `test_pending_interactions_router.py` | Active |
| REG-104 | Comment-mutation extraction contract drift across sources/providers | Action-item comment extraction payloads were loosely shaped (`task_key`/`posted_comment_text` fields), making cross-provider onboarding brittle and allowing malformed events to enter queue/job paths silently | Introduced canonical `CommentMutationEvent` normalization/validation contract, queue payload embedding (`comment_event`), and job-side canonical validation/fallback with regression coverage (`test_comment_mutation_event.py`, `test_action_items_extraction_queue.py`, `test_action_items_extraction_job.py`) | Active |
| REG-105 | Jira webhook comment-create events not emitting canonical extraction events | Action-item auto-extraction canonical contract could be bypassed for Jira-direct comments if webhook path did not produce canonical event metadata (provider/source/source_event_key/actor resolution) | Jira webhook handler now emits canonical comment-mutation payload to action-item extraction queue for comment-create events with best-effort actor resolution and stable source-event key, covered in `test_jira_webhooks_recommendation_dirty.py` | Active |
| REG-106 | Cortex latency bottleneck diagnosis blind spot | Slow turns lacked deterministic per-turn timing breakdown, making it hard to isolate whether latency was LLM, tool execution, or response delivery | Added structured Cortex runtime timing summary (`turn_total_ms`, `executor_ms`, `llm_ms_estimate`, `tool_ms_total`, `tool_ms_by_name`, `delivery_ms`) emitted in orchestrator debug logs using executor metadata (`api/app/agent_runtime/executor.py`, `api/app/cortex/orchestrator.py`) | Active |
| REG-107 | Jira write latency from repeated no-active-sprint checks | Multi-step Jira writes repeatedly fetched active sprint and received identical `no active sprint` responses, adding avoidable per-turn latency | Jira adapter now applies short TTL Redis cache for negative active-sprint lookup only (`no_active_sprint`) and skips repeat board sprint calls during TTL; covered by `test_jira_adapter_no_active_sprint_cache.py` | Active |
| REG-108 | LiteLLM async callback warning noise across per-job event loops | RQ Cortex jobs run with `asyncio.run()` per job (new loop each run); LiteLLM global logging worker loop rollover could leave stale queued coroutines and emit `RuntimeWarning: coroutine 'Logging.async_success_handler' was never awaited` | Added one-time LiteLLM logging-worker stabilization patch in runtime seed path to close stale queued coroutines on event-loop transition before queue reset (`api/app/cortex/llm/litellm_config.py`) | Active |
| REG-109 | Task-comment status updates auto-created noisy action items | ProMarshal task comment updates (for example `completed`, `testing completed`) were always queued for extraction and could create false-positive action items not representing external follow-up work | Auto-extraction now prioritizes raw comment text, applies deterministic status/progress prefilter, and uses fail-closed LLM classifier gating for ambiguous comments (`is_action_item` + confidence threshold) before create; explicit external follow-up cues still create action items (`api/app/action_items/extraction.py`, `api/app/tasks/service.py`, `api/tests/test_action_items_extraction_job.py`) | Active |
| REG-110 | Action-item list default scope returned closed/cancelled items unexpectedly | Conversational list requests without explicit status filter (for example `list my action items`) returned all statuses, creating mismatch with open-by-default expectation used for task reads | Action-item list defaults to `open` for Cortex/Slack list intents unless user explicitly asks for all/closed/cancelled; added alias handling (`status=all|any|*`, `status=closed`) and regressions in `test_cortex_action_item_tools.py` and `test_slack_command_actionitem.py` (`api/app/cortex/tools/providers/action_item_tools.py`, `api/app/integrations/slack/command_handlers.py`, `api/app/action_items/service.py`) | Active |
| REG-111 | Cortex task-read requests incurred avoidable LLM latency | Deterministic read intents (for example `list my tasks`, `how many tasks do i have`) still went through full agent runtime LLM/tool loop, producing high p95 latency despite deterministic intent parse availability | Added deterministic `jira_search_tasks` fast-path in orchestrator with explicit execution context, metadata/telemetry parity (`_persist_turn_metadata`, `cortex_tool_outcomes`), and fail-safe fallback to existing runtime for non-eligible intents; covered by `test_cortex_task_read_fastpath.py` | Active |
| REG-112 | Cortex worker Mongo hot-path overhead | Every Cortex RQ job performed proactive Mongo ping/reconnect checks and repeated index initialization on reconnect paths, adding avoidable per-turn overhead and noisy reconnect churn | Worker bootstrap now uses null-check-only Mongo readiness (`db is None -> connect`) and `connect_to_mongo` initializes indexes once per process lifecycle; covered by `test_cortex_worker_mongo_bootstrap.py` and `test_database_connect_bootstrap.py` | Active |
| REG-113 | Non-fork worker event-loop/connection churn and stale loop-bound Mongo reuse | Non-fork Cortex workers recreated event loop per job (`asyncio.run`) and closed Mongo after each run, preventing pool reuse; after loop reuse hardening, stale loop-bound Mongo clients could still be reused after loop replacement and raise `RuntimeError: Event loop is closed` | Worker entrypoint uses reusable per-process event loop for non-fork runtimes; Mongo readiness now includes loop-binding mismatch recovery (`close + reconnect`) while preserving healthy reuse fast path; database tracks/reset client loop binding on connect/close. Covered by `test_cortex_worker_runtime_entrypoint.py`, `test_cortex_worker_mongo_bootstrap.py`, and `test_database_connect_bootstrap.py` | Active |
| REG-114 | Mongo pool sizing drift under worker scaling | Fixed hardcoded pool parameters could over-allocate connections per worker at scale and make tuning risky across environments | Mongo client pool/timeouts are now settings-driven (`MONGODB_*`) with bounds validation in config; startup log reports effective pool settings to aid environment tuning | Active |
| REG-115 | Fast-path read compose produced deterministic dump and transport instability vs runtime behavior | Deterministic read fast-path used a separate compose transport (`compose_user_response`/gateway) with fallback-draft anchoring and timeout drift, causing style mismatch and long compose waits despite successful deterministic tool execution | Fast-path compose now reuses shared runtime response path (`shared_agent_runtime.run` with `tools=[]`) using prompt-builder-generated `task_assistant_read` instructions + compact grounded outcome payload. Added explicit compose timing/reason telemetry and hard compose timeout fallback guard (`runtime_failed/timeout/exception/empty/fallback_same_text`). Covered by `test_cortex_task_read_fastpath.py` and existing prompt-builder/runtime regressions. | Active |
| REG-116 | Scope policy drift between Cortex runtime path and legacy facade | Team workload-analysis prompts (for example `how is workload distributed among team`) could be blocked as `out_of_scope` in live Cortex while legacy facade/tests treated them as in-scope analytics | Canonicalized analytics scope rules in `app.domain.scope.gate` (`pm_analytics_query`, PM-definition and non-PM workload guards) and removed duplicate special-case logic from `app.promarshal.scope_policy` so both paths share one source of truth; expanded analytics lexicon/context to include team burnout/capacity/distribution and action-item burden prompts | Active |
| REG-117 | Delivered out-of-scope replies marked as worker job failures | Scope-policy refusals could be delivered to user correctly but still raise `cortex_orchestrator_failed:out_of_scope`, polluting job failure metrics and logs | Cortex worker `_process_one` now treats delivered terminal policy outcomes (`error=out_of_scope` + `metadata.delivered=true`) as handled non-fatal outcomes; true runtime/tool failures continue to raise | Active |
| REG-118 | Layer B tool-call history loss and weak timeout ceiling risk | Gateway Layer B transport could drop assistant `tool_calls` from request history in multi-step loops, and gateway completion timeout handling relied only on LiteLLM timeout semantics | Extended gateway request message contract to carry assistant `tool_calls`, preserved/mapped these calls in Layer B adapter + LiteLLM payload serialization, and added outer `asyncio.wait_for` hard timeout ceiling in gateway transport with focused tests (`test_gateway_tool_call_message_mapping.py`, `test_litellm_gateway.py`) | Active |
| REG-119 | Jira self-scope assignee collision on duplicate member names | `my tasks` reads could over-include tasks from same-name teammates because assignee-name fallback was combined with email in repository filters | Jira self-scope now avoids name-based filtering when actor email/account-id exists, and repository assignee precedence is strict (`ID -> email -> name`) so name never widens an email-filtered query; covered by `test_cortex_jira_search_self_scope_guardrail.py` and `test_jira_task_read_repository.py` | Active |
| REG-120 | Ambiguous Cortex write prompt guessed unrelated target ID | Ambiguous follow-up write prompts (for example `assign both to <owner>`) could execute with a model-inferred task ID not explicitly present in user input | Cortex pre-execution write-target grounding guard now blocks write calls when target identifier args are not present in latest user message, returning generic clarification instead of executing guessed mutations; covered by `test_cortex_orchestrator_confirmation_policy.py` | Active |
| REG-121 | Dramatiq queue jobs reused closed/loop-mismatched Mongo client under mixed per-job lifecycle | Queue workers mixed per-job close/reconnect with process-level reuse, causing failures like `Cannot use MongoClient after close` and reconnect churn under Dramatiq async runtime | Standardized worker-path Mongo readiness to health-checked reuse (`ensure_mongo_ready`), removed per-job close paths in queue consumers (Cortex/Jira/Cadence/PM summary/Slack/action-item extraction), and removed loop-ID-based reconnect checks in favor of runtime-owner + ping health checks | Active |
| REG-122 | PM summary stale snapshot reuse after workitem mutation | Jira sync/webhook/connect mutation paths could invalidate Redis PM cache but still allow stale Mongo snapshot fallback on `/pm-board-summary`, causing Pulse/PM Board to show stale work items/recommendations until manual refresh | Added PM summary dirty-marker contract (`pm_board_summary:dirty:v1:{project_id}`), snapshot-bypass-on-dirty in PM summary read path, and mutation-triggered async recompute enqueue wiring for Jira connect/sync/webhook update/delete paths; Pulse Work Item now revalidates with `/project-health` when initial `work_items` is empty/stale | Active |
| REG-123 | Cadence summary extraction created noisy/misassigned action items | Cadence `key_input`/overall summary text could be converted into action items without intent gating, with forced actor-owner assignment and unstable dedupe key, producing status-note noise and duplicate/misattributed items | Cadence extraction now applies status-note prefilter + classifier/external-action cue gating, removes forced owner assignment (explicit-owner resolution only), prefers actor-authored overall update over synthesized key input, and uses stable session+text hash dedupe key (`cadence:{session_id}:{hash}`) | Active |
| REG-124 | Action-item create owner intent missed natural `for <name>` phrasing | Cortex action-item create prompts like `create action item for Prabhu ...` could leave assignee unresolved even when owner was clearly specified, because owner intent parsing only trusted narrow assign/owner phrases and ignored create-for variants | Added shared owner-intent parser across Cortex create + extraction, create-flow deterministic-first owner hint resolution with weak-intent false-positive guard, and LLM fallback owner hint extraction when deterministic parse cannot recover hint; regression coverage added in `test_cortex_action_item_tools.py` | Active |
| REG-125 | Owner-intent parser overfitting created phrase-by-phrase regressions | Expanding deterministic regex coverage for one wording could break other regional phrasings and cause unstable assignment behavior | Simplified deterministic owner parsing to high-confidence assignment patterns only (`assign to`, `owner is/as`, explicit `create action item for <member>`), and shifted uncertain phrasing to strict JSON LLM fallback with guarded unresolved outcome when identity resolution is not found; regression coverage maintained in `test_cortex_action_item_tools.py` | Active |
| REG-126 | Action-item title retained command wrapper text in create flow | Prompts like `create an action item to ... and assign to ...` could persist wrapper text in title because prefix-strip regex missed article variants and cleanup only executed on limited temporal backfill conditions | Hardened create-title normalization in Cortex tool path to strip wrapper prefixes (`create an action item`, polite prefixes) and assignment/owner suffixes before persistence while preserving temporal/action content; added regression test `test_create_tool_strips_create_wrapper_and_assignment_suffix_from_title` in `test_cortex_action_item_tools.py` | Active |
| REG-127 | Dramatiq Mongo runtime-owner mismatch caused cross-queue connection churn and intermittent action-item create failures | Action-item extraction ensured Mongo with runtime owner token `dramatiq_event_loop_thread` while Cortex/actors used `dramatiq_event_loop_thread:{pid}`, triggering `ensure_mongo_ready` runtime-owner mismatch reconnects (`Closed MongoDB connection`) across shared worker process and causing transient tool/service failures | Unified extraction runtime-owner token to `dramatiq_event_loop_thread:{pid}` to match Cortex/actor owner format and prevent cross-thread forced reconnect churn in shared Dramatiq worker process | Active |
| REG-128 | Cadence clarification parser/routing mismatch trapped backlog/status replies in clarification loops or out-of-scope fallback | Runtime parsing adapter passed `parser_context` kwargs, but Cadence parser adapters only accepted positional `text`, causing parse-invalid clarification loops; DM handler also lacked explicit routes for `awaiting_backlog_clarification` and `awaiting_status_clarification`, allowing misrouting to agent-runtime fallback | Updated Cadence parser adapter signatures to accept passthrough kwargs and added explicit clarification-state routing in `handle_cadence_dm`; added regression coverage in `test_cadence_orchestrator_extraction.py` for parser-context compatibility and clarification-state routing | Active |
| REG-129 | Comment-mutation owner assignment missed natural `name to <action>` phrasing | Task comments like `Sridevi to connect with DevOps team ...` created follow-up action items but defaulted to unassigned because owner-intent parser only trusted explicit `assign to/owner is/create ... for` cues | Extended shared owner-intent parser with bounded `name to <verb>` detection (start/separator anchored + disallowed-command guard) so extraction/tool flows resolve owner via project identity resolution when unambiguous; added regression coverage in `test_action_items_extraction_job.py` and `test_cortex_action_item_tools.py` | Active |
| REG-130 | Owner DM poll interception overrode normal Cortex intent handling | Owner free-text messages containing broad poll-like terms (for example `ask ... team ...`) could be intercepted by legacy team-poll ingress before Cortex tool arbitration, causing poll flow to trigger for non-poll requests | Added config-gated owner free-text routing (`team_poll_owner_free_text_via_cortex`, default enabled), disabled legacy ingress interception when enabled, and introduced Cortex Team Poll tools (`team_poll_create/status/close`) with confirmation required for create | Active |
| REG-131 | Team operational broadcast asks blocked as out-of-scope before tool planning | Valid project-operational group check-in asks could be classified out-of-scope by scope gate/classifier, resulting in `runtime_tool_count=0` and refusal response instead of reaching runtime tool selection | Scope gate now recognizes broadcast-operation intent shape (request + audience + response/update semantics or project entity) as project-related; stage-2 prompt also explicitly treats team/group update-check asks as in-scope action; regression coverage added in `test_domain_scope_gate.py` and `test_scope_classifier_prompt.py` | Active |
| REG-132 | Pending Team Poll reminder gap after Cadence completion | When Cadence had routing precedence, unresolved Team Poll responses were not proactively re-prompted after interactive Cadence completion, leaving polls pending until unrelated user activity or scheduler nudge | Added post-Cadence reminder hook for unresolved Team Poll pending interactions (Slack identity scoped), deterministic multi-poll selection, cooldown idempotency capped by remaining poll TTL, and Redis fail-open behavior with coverage in `test_team_poll_post_cadence_reminder.py` | Active |
| REG-133 | Pending Team Poll relevance false-negative for short natural replies | Valid short free-text answers (for example `no i wont be`) could be treated as unrelated when embedding similarity to the full question was low, causing repeated reminder loops | Relevance decision chain now uses deterministic safety + embedding high-relevant accept + mandatory LLM fallback classifier for all non-high-relevant cases to detect possible answers; non-answer paths remain pending-poll reminder flow. Covered by `test_team_poll_relevance.py` | Active |
| REG-134 | Scheduler metadata growth and stale cleanup drift | Centralized scheduler control-plane metadata and ephemeral routing rows could accumulate without bounded retention (for example `schedule_runs`, stale `channel_index`, summary artifacts), while cleanup execution could compete with user-facing jobs | Added lane-aware dispatcher execution ordering (product then maintenance), global/per-project retention cleanup jobs (`schedule_runs_cleanup`, `channel_index_cleanup`, `project_summary_cleanup`), TTL fallback on `pending_interactions.expires_at`, and cleanup indexes/retention configs with scheduler/cron regression coverage (`test_cron_dispatcher.py`, `test_scheduler_cleanup_jobs.py`) | Active |
| REG-135 | Action-item owner/reminder regression in noisy payload and follow-up notification | Owner resolution missed `assign to me` when natural-language text arrived in `owner_user_id` instead of message context, and follow-up reminder formatting crashed due to invalid multi-arg `list.append(...)` call | Shared owner-intent parser now reparses noisy `candidate_owner_hint` as free text when primary utterance is missing; follow-up reminder lines use `extend([...])` for multi-line additions. Covered by `test_cortex_action_item_tools.py::test_create_tool_resolves_noisy_owner_hint_with_assign_to_me_phrase` and `test_scheduler_action_item_jobs.py::test_followup_actor_reminder_includes_assignment_suggestions` | Active |
| REG-136 | Action-item extraction retried permanent project lookup failures | Queue extraction jobs raised and retried indefinitely on `Project not found`, causing noisy retries for invalid/stale payloads that cannot recover | Extraction now treats `Project not found` as deterministic skip (`ok=true`, `reason=project_not_found`) and job entrypoint returns non-retryable failures (`retryable=false`) without raising; covered by `test_action_items_extraction_job.py::test_task_comment_job_skips_when_project_not_found` and `test_action_items_jobs.py` | Active |
| REG-137 | ACTION reference routed to Jira assign tool and owner parse over-captured target refs | Prompts like `assign ACTION-10 to <owner>` could be misrouted to `jira_assign_task`, and owner parsing could incorrectly capture `action-10 to <owner>` as owner text in follow-up assignment turns | Canonical action-item display key prefix is `ACTION-<n>` only; action-item ref resolvers accept only `ACTION-*`; Jira assign tool now hard-rejects `ACTION-*`; owner-intent parser now uses entity-agnostic `assign <any target> to <owner>` extraction so only owner phrase is captured; pending action-item clarification handler now reuses shared owner-intent parser (no local prefix-strip parser). Covered by `test_cortex_jira_assign_guard.py`, `test_action_item_owner_intent.py`, `test_action_item_pending_handler.py`, and updated action-item resolver tests | Active |
| REG-138 | Cadence summary day shown as previous date for project-local early-hour runs | Cadence summary `date` and default lookup day were derived from UTC, so runs shortly after local midnight (for example 01:00 IST) appeared under the previous day in UI/DM | Cadence summary generation and read-path default now use project-local timezone day semantics; summary docs persist `project_timezone` + `date_basis=project_local`, active-session window checks align to project-local day boundaries converted to UTC, and a one-time backfill script updates historical `cadence_daily_summary` dates (`api/scripts/backfill_cadence_summary_dates.py`) | Active |
| REG-139 | Timezone update left one stale daily schedule fire in old timezone | Updating `projects.timezone` changed canonical project timezone but existing project daily schedules in `project_schedules.next_run_at` could remain computed with old timezone until next run, causing one incorrect fire window | Project update now detects timezone change (`old != new`) and immediately realigns all project-scoped daily schedules (`schedule_spec.mode=daily`) by recomputing `next_run_at` with the new timezone (`realign_project_daily_schedules_for_timezone`), with regression coverage in `test_project_timezone_schedule_realign.py` | Active |
| REG-140 | JWT migration caused mixed-client auth breakage | During auth hardening rollout, some clients still sent actor identity via `X-User-Id`/`user_id` without bearer token, causing immediate 401s before all callers were migrated | Transitional auth gate in `get_current_user`: when `jwt_enforcement_enabled=false`, accept `X-User-Id` or `user_id` fallback; when `true`, enforce strict bearer-only auth. Guard with auth-dependency unit coverage (`test_auth_deps.py`) and staging smoke coverage for both modes before production cutover | Active |
| REG-141 | Project list identity mismatch allowed cross-user enumeration attempt | `GET /api/projects` accepted caller-supplied `user_id` query while route auth only asserted caller was authenticated, allowing mismatch attempts between token identity and query identity | `list_projects` now binds to JWT actor identity, rejects mismatched `user_id` query with `403`, and always lists projects for the authenticated user. Frontend calls migrated away from `?user_id=` for protected listing paths; regression guard added in `test_frontend_auth_migration_guards.py` | Active |
| REG-142 | Action-item JWT migration schema mismatch returned 422 for token-auth payloads | Action-item create/update/close schemas still required body `user_id` even though router enforces actor from JWT and injects token identity, causing frontend token-only calls (e.g., color update PATCH) to fail validation | Action-item request schemas now treat `user_id` as optional and router remains authoritative for actor binding (`payload_dict["user_id"] = actor_user_id`), with regression tests covering token-only create/update/close payloads in `test_action_items_router.py` | Active |
| REG-143 | Action-item owner resolution failed on trailing punctuation in owner hint | Conversational utterances like `assign to sridevi.` carried trailing punctuation into identity resolution and could return `owner_resolution_failed` despite an unambiguous active member match | Shared identity-hint normalization now strips surrounding/trailing conversational punctuation before member resolution; regression coverage added in `test_identity_resolver.py` (`test_resolves_by_name_when_hint_has_trailing_punctuation`) | Active |
| REG-144 | JWT enforcement cutover drift between auth modes and internal exchange path | Environment cutovers can regress one of four critical ingress expectations (unauth 401, auth 200, list mismatch 403, internal token exchange 200) and remain undetected until production traffic | Use repeatable cutover smoke script `scripts.auth_cutover_smoke` in staging and production as release gate, validating protected route auth behavior and internal token exchange in one run | Active |
| REG-145 | Frontend production build failed on conditional auth headers union typing | A conditional headers object (`{ Authorization: string } | { Authorization?: undefined }`) caused Next.js TypeScript build failure in `fetch(..., { headers })` due `HeadersInit` incompatibility | Explicitly type conditional auth header object as `HeadersInit` in tasks due soon widget; prevents optional-undefined header union inference during production type-check | Active |
| REG-146 | Project onboarding create returned opaque 500 and could fail with empty actor id | Dashboard/project-setup onboarding passed only `session.user.userId`; when absent, create payload sent empty `userId`, and web route collapsed backend errors into generic 500 | Normalize onboarding actor id fallback to `session.user.userId || session.user.id`, and forward backend create-project status/error through `web/lib/api.ts` + `/api/projects/create` route for precise client-visible failures and faster diagnosis | Active |
| REG-147 | Onboarding create route dropped bearer token under JWT enforcement | `/api/projects/create` proxied project creation without forwarding session `backendToken`, so backend-protected `/api/projects` returned `401 Missing Authorization header` even for logged-in users | Next route now requires authenticated session with `backendToken` and forwards `Authorization: Bearer <backendToken>` via `createProject(..., backendToken)` to backend create endpoint | Active |
| REG-149 | Project delete authorization gap | Members with project access could reach delete API path because delete permission was not owner-scoped in backend service contract | Owner-only enforcement in `DELETE /api/projects/{project_id}` with JWT actor binding and explicit `403`/`404`/`409` semantics | Active |
| REG-150 | Project settings visibility leak to non-owners | Control Panel/Project Settings were visible and deep-linkable for non-owner members, exposing owner-only management surfaces in UI | Owner-only Control Panel gating + URL/state sanitization fallback to Pulse for non-owner Control Panel deep links | Active |
| REG-151 | Project hard-delete orphan artifacts | Deleting a project row could leave cross-store artifacts (brain collection, scheduler rows, uploads, redis runtime keys, slack message metadata, stale context keys) | Staged project deletion orchestrator with required cleanup (schedules/brain/uploads), best-effort secondary cleanup, per-project delete lock, and idempotent replay-safe flow | Active |
| REG-152 | Recommendation scheduler stale schedule execution after project delete | Recommendation full/incremental executors could still run for stale schedule records during delete races and proceed without explicit project existence guard | Scheduler recommendation executors now resolve project existence first and return deterministic `{"skipped": true, "reason": "project_not_found"}` without invoking recompute orchestration | Active |
| REG-153 | Cadence/digest weekend and owner follow-up policy drift | Cadence and action-item digest could send during weekends, and unresolved follow-up escalation always targeted owner even when teams wanted owner suppression | Per-project schedule policy flags (`skip_weekends`, `ignore_followup_for_owner`) enforced in schedule API persistence, scheduler next-run/runtime guards, and action-item follow-up escalation logic with focused scheduler/action-item tests | Active |
| REG-154 | Slack `user_not_member` from stale member identity mapping | Slack reconnect/disconnect preserved stale `members[].integration_ids.slack_user_id`, causing strict resolver mismatch with live Slack event user ID and sticky `user_not_member` failures | Strict ID-first resolver now uses controlled email fallback on miss and auto-heals member Slack ID cache; disconnect clears member Slack identity fields; reconnect/member-sync use overwrite-safe healing with regression tests (`test_slack_identity_healing.py`) | Active |
| REG-155 | Work-item hard cutover read-path empty window | Runtime cutover from `entity_type="task"` to `entity_type="work_item"` could return empty PM/Cortex/health reads when migration is incomplete | Enforce cutover runbook gate + internal-only validation window; add migration smoke suite verifying read parity before/after migration and publish checkpoint status via migration-progress endpoint | Active |
| REG-156 | Premature canonical type activation in health/recommendations | Enabling `work_item_type`-driven logic before backfill completion could misclassify or omit historical records | Phase-4 activation gate requires >=99.9% `work_item_type` backfill coverage and canonical parity checks (`issue_type` vs mapped `work_item_type`) before enablement | Active |
| REG-157 | Planner/tool fallback reintroduced deprecated `task_*` capabilities | Fallback IDs/tool names could silently route to legacy task semantics after canonical migration | Replace planner/tool fallback defaults with `workitem_*` IDs; add capability-registry startup assertion that blocks boot if `task_*` runtime fallbacks remain | Active |
| REG-158 | Project records not upgraded for canonical capability/config schema | Existing project docs could retain legacy task-centric capability/config values and break tool routing after cutover | Add project-record migration step for control-plane fields in `projects` collection (capability IDs, defaults, migration markers) with idempotent backfill and verification report | Active |
| REG-159 | Cross-domain references drift after work-item ID/type migration | Action-item `related_type="work_item"` references could fail lookup if resolver still expects `entity_type="task"` | Add cross-reference integrity sweep after migration (`related_id` resolvability checks) and fail deployment gate on unresolved reference threshold breach | Active |
| REG-160 | Neo4j entity-label divergence after canonical migration | Graph nodes could split across `task` and `work_item` labels, producing incomplete graph queries and analytics drift | Include Neo4j relabel/query migration in runbook with post-migration graph parity checks and label-level count assertions | Active |
| REG-161 | Work-item provider-type fidelity loss for custom project types | Custom provider-native types could collapse into incorrect canonical buckets and degrade recommendations/cadence behavior | Preserve `provider_type` as source-of-truth display/write-back field and enforce per-project type-registry mapping tests including custom type passthrough and canonical fallback classification | Active |
| REG-162 | Work-item date mutation mismatch across providers | Start/due-date write operations could fail or write wrong fields (for example Jira custom start-date field IDs) under canonical work-item tools | Add provider-specific date-field discovery + validation tests for `workitem_update_start_date` and `workitem_update_due_date` (set/clear) with failure-safe error contracts | Active |
| REG-163 | Health KPI scope drift from provider-native type names | Health summary KPI scope could exclude valid non-story standard work items when provider label differs (for example `Issue`, custom standard aliases), causing undercounted PM/Pulse summary totals | Compute KPI summary scope from canonical `work_item_type=standard` semantics in health service and preserve backward-friendly scope output; regression coverage in `test_work_item_type_health_and_capability_semantics.py` | Active |
| REG-164 | Graph canonical-type projection drift | Neo4j work-item nodes could miss canonical type properties after cutover, leaving graph queries dependent on legacy `issue_type` only and risking analytics divergence | Persist `work_item_type` and `provider_type` on graph nodes in Neo4j sync path while retaining legacy `issue_type` compatibility field; covered by normalizer/type-contract tests (`test_jira_normalizer_contract_k.py`) and migration runbook graph checks | Active |
| REG-165 | Deterministic action-type canonicalization drift | Planner/deterministic parsing could diverge when legacy action types (`create_task`, `assign_task`) remain in some pattern specs while runtime expects canonical work-item action types, causing step-routing inconsistencies | Canonicalize deterministic action types to `create_work_item`/`assign_work_item` in bundle/planner paths and keep model-layer alias normalization for legacy inputs; regression coverage in deterministic/planner tests (`test_deterministic_pattern_registry.py`, `test_deterministic_interpretation_service.py`, `test_cortex_capability_routing.py`) | Active |
| REG-166 | Control-plane migration non-idempotent rewrites | Re-running work-item migration could repeatedly rewrite already-migrated project control-plane documents because marker/update fields were mutated before no-op comparison, creating unnecessary write churn and noisy audit signals | Make `_migrate_project_control_plane` idempotent by checking canonical-term replacement and migration-marker drift before mutating timestamps; add focused migration-service tests for legacy-change and already-migrated dry-run paths (`test_work_item_migration_service.py`) | Active |
| REG-167 | Rehearsal dry-run gate false failure | Migration rehearsal script could fail dry-run even when behavior was expected, because it enforced execute-mode completion checks against write-dependent verification checks that intentionally fail before migration writes | Add mode-aware rehearsal exit gating: dry-run allows write-dependent failed checks while still failing on unexpected integrity/report errors; add focused script-gate tests (`test_work_item_migration_rehearsal_script.py`) | Active |
| REG-168 | Migration start-path gate drift | Work-item migration kickoff could regress runbook control-plane behavior (already-running lockout, lock acquisition failure, enqueue failure handling, or queued acceptance contract), causing inconsistent ops behavior under cutover pressure | Add focused migration-service start-path tests for `already_running`, `lock_unavailable`, `enqueue_failed`, and successful `queued` flows (`test_work_item_migration_service.py`) to lock response and status contracts | Active |
| REG-169 | Verification report gate drift | Migration verification-report generation could regress key gate semantics (DB-unavailable error contract, baseline all-pass behavior on clean empty scope, or Neo4j remaining-node check failure signaling), reducing confidence in cutover readiness gates | Add focused verification-report tests in migration service for DB-unavailable response, empty-project pass contract, and explicit Neo4j remaining-node failure check (`test_work_item_migration_service.py`) | Active |
| REG-170 | Migration job state/checkpoint transition drift | `run_work_item_migration_job` could regress fail/complete state transitions or checkpoint progression (preflight -> neo4j_migration -> finalize), reducing trust in ops progress visibility and cutover gating | Add focused migration-job execution-path tests for DB-unavailable failure semantics and empty-scope successful completion with checkpoint phase coverage (`test_work_item_migration_service.py`) | Active |
| REG-171 | Project-level migration failure handling drift | A single project migration exception during run execution could fail to increment failure counters or surface deterministic project-scoped error context, reducing operator ability to triage cutover failures | Add focused migration-job test asserting failed status, project failure counters, records_failed increment, and `last_error` project context when `_migrate_project_brain_collection` raises (`test_work_item_migration_service.py`) | Active |
| REG-172 | Migration ops-route contract drift | Internal migration ops endpoints (`/status`, `/start`, `/report`) could regress service passthrough/validation behavior, breaking runbook automation and operator workflows | Add focused route-level tests for status/start/report passthrough and request-bound validation limits (`project_limit` bounds) in `test_work_item_migration_routes.py` | Active |
| REG-173 | Neo4j migration gate false-pass on connectivity failure | Verification/report paths could treat Neo4j-unavailable as zero remaining nodes, producing a misleading PASS and masking incomplete graph migration validation | Neo4j migration now returns explicit unavailable sentinel (`-1`), report gate fails on unavailable state, and migration run marks deterministic `neo4j_unavailable`; added focused service tests for report and run-job behavior (`test_work_item_migration_service.py`) | Active |
| REG-174 | Jira create-type cache stale miss false-reject | Work-item create pre-validation could incorrectly reject a newly added Jira issue type when cached allowed-create metadata is stale or incomplete | Added no-expiry cache-first validation with live refresh-on-type-miss before reject; cache persisted in project-scoped Brain metadata doc and covered by focused tests (`test_jira_create_metadata_cache.py`) | Active |
| REG-175 | Jira subtask parent-constraint violation at create | Sub-task create could proceed with invalid parent relationships (parent on non-subtask create, cross-project parent, or subtask-as-parent), leading to avoidable Jira write failures and unclear runtime errors | Added create-time parent-constraint hardening in Jira adapter (subtask-only parent key, same-project parent, non-subtask parent) with focused tests (`test_jira_create_parent_constraints.py`) | Active |
| REG-176 | Linear provider read-dispatch drift | Provider-agnostic read dispatcher could route to provider without concrete repository implementation, causing fallback ambiguity or Jira-coupled behavior when Linear is active | Added `LinearTaskReadRepository` and dispatcher registration with focused list/search/get repository coverage (`test_linear_task_read_repository.py`) and invariant update for active provider repository set | Active |
| REG-177 | Deterministic subtask-parent extraction loss | Natural-language create prompts like `create subtask X under PRM-42` could lose parent reference during deterministic extraction/planning, causing missing-parent create failures despite explicit user input | Extended Jira create deterministic pattern to extract `parent_external_id` and forward via planner args; added focused slot and cadence/cortex parity tests (`test_work_item_create_type_support.py`, `test_cadence_deterministic_interpreter.py`) | Active |
| REG-178 | Pending parent-clarification flow stalled on iterative candidate navigation | Subtask parent clarification previously stored only top-3 options and did not persist iterative list-state, so follow-ups like `show more`/`list all`/numeric option selection across pages could degrade into unrelated intent handling | Persist pending parent candidate pool + view state in `pending_interactions.context_payload`, support iterative commands (`show more`, `previous`, `list all`, `top`) and option/key selection on current view, and remove hardcoded key examples in clarification copy; covered by `test_work_item_parent_pending_handler.py` | Active |
| REG-179 | Post-migration legacy `task` writes excluded from canonical parent/read flows | Newly written Jira rows in legacy `entity_type=\"task\"` shape could be skipped by canonical `work_item` query paths (for example parent suggestion), even when records existed in Brain | Enforce write-time canonicalization (`entity_type=work_item`, canonical `work_item_type`, populated `provider_type`) across BrainSync/webhook/import upserts and provide one-time repair script for leftover legacy rows (`api/scripts/repair_work_item_entity_type.py`) with focused tests in brain-sync/webhook/import suites | Active |
| REG-180 | Jira subtask type label mismatch and clarification-text drift | Subtask create pre-validation could reject valid project labels (`Sub-task` vs `Subtask`) and runtime could rephrase tool-provided clarification prompts, dropping actionable follow-up hints (for example `show more`/`list all`) | Jira create-type validation now uses canonical token matching with allowed-label resolution (write uses provider’s exact allowed label), pending-parent fallback messaging distinguishes type mismatch from parent mismatch, and executor/orchestrator now preserve deterministic clarification `response_text` verbatim when tool returns `requires_clarification`; covered by `test_jira_create_metadata_cache.py`, `test_work_item_parent_pending_handler.py`, and `test_cortex_task_read_fastpath.py` | Active |
| REG-181 | Parent-clarification option ambiguity and verbose guidance drift | Paged parent suggestions used local 1..N numbering while header showed global ranges (for example showing 4-13), causing selection ambiguity; guidance copy was overly verbose and inconsistent across turns | Parent clarification now uses absolute option numbering aligned with displayed range, compact command footer (`Commands: ...`), concise candidate rows with provider type/status, and deterministic subtask-create success summary including assignee/status; covered by `test_work_item_parent_pending_handler.py` | Active |
| REG-182 | Subtask parent-clarification dropped assignee intent | `create subtask ... assign to me` could lose assignee when parent clarification was required because pending payload did not always carry resolved assignee account id to final create step | Pending parent handler now derives assignee intent from stored request context (`requested_assignee_hint` / original user message), resolves self alias to actor email, and resolves Jira account id before final create; covered by `test_work_item_parent_pending_handler.py` | Active |
| REG-183 | Parent clarification bypassed canonical create tool path | Parent-selection follow-up previously executed direct creation inside pending handler, bypassing standard create-tool execution path and causing response/behavior drift versus normal create flow | Pending parent handler now resolves parent only and emits resume payload; Slack router resumes through Cortex create-tool path with enriched parent args so normalization/assignee/rules/formatting stay centralized in one path; covered by `test_work_item_parent_pending_handler.py` | Active |
| REG-184 | Pending parent flow remained subtask-specialized and non-reusable | Parent clarification copy/entrypoint remained subtask-branded, limiting reuse for future parent-required work-item relations across providers | Introduced generic work-item parent clarification queue entrypoint and generic child-parent resume contract fields while preserving backward wrapper for transition; covered by `test_work_item_parent_pending_handler.py` | Active |
| REG-185 | Parent-clarification follow-up drifted from canonical create flow | Parent-selection follow-up could execute through a side-channel resume path, causing behavior/format differences versus direct create with explicit parent | Removed tool/args resume side-channel and re-entered Cortex via canonical `resume_message_text` only, so follow-up parent resolution uses the same create flow as direct requests; covered by `test_pending_interactions_router.py` and `test_work_item_parent_pending_handler.py` | Active |
| REG-186 | Post-clarification create title/sprint wording quality drift | Create confirmations could include parser glue in title (`for ...`) and redundant sprint wording in backlog insight text | Jira create input now strips deterministic leading `for` title artifact and normalizes sprint backlog insight wording; covered by `test_work_item_create_type_support.py` | Active |
| REG-187 | PM/Pulse work-item totals and task-widget list drift after canonical cutover | PM/Pulse totals could still read legacy `summary.total_tasks` while task widgets queried Jira records without canonical `entity_type` filter, causing inconsistent counts/lists despite migrated data | PM/Pulse task-widget queries now require `entity_type=\"work_item\"` in project service methods, PM summary mapping uses `health.work_item_summary` as primary source, and Pulse initial hydration zero-state guard reads canonical `work_item_summary.total` with legacy fallback | Active |
| REG-188 | Jira Sync-Now PM Board stale snapshot/cache reuse | After `Sync Now`, PM Board could still render stale summary because async recompute was queued but snapshot/cache from prior state could still be served before recompute completion | Jira `Sync Now` now enforces PM force-rebuild contract: clear Redis PM summary cache, delete PM snapshot doc (`project_insights_snapshot/project-insights:v1`), mark summary dirty, then enqueue recompute; covered by `test_jira_sync_pm_board_rebuild.py` | Active |
| REG-189 | Subtask parent clarification suggested invalid hierarchy types | Parent clarification could rank broad provider types (for example Story/Epic) ahead of project-valid parent types because candidate prefilter relied only on generic non-subtask exclusion and ignored provider/project parent rules | Jira create-metadata cache now stores `parent_rules` alongside allowed create types, and pending parent clarification applies provider parent-rule filtering before LLM ranking (with fail-safe fallback when metadata is unavailable); covered by `test_jira_create_metadata_cache.py` and `test_work_item_parent_pending_handler.py` | Active |
| REG-190 | Parent-rule filtering drift under missing/incomplete Jira metadata | Parent clarification could become inconsistent when metadata lacked `allowed_parent_provider_types` (or stale cache), causing over-broad suggestions and non-deterministic fallback behavior across projects | Added shared parent-rule resolver with normalized metadata + Jira-safe fallback merge, normalized parent-rule persistence bounded by project allowed-create types, and explicit sync-now metadata refresh trigger to hydrate cache early; covered by `test_provider_parent_rules.py`, `test_work_item_parent_pending_handler.py`, and `test_jira_sync_pm_board_rebuild.py` | Active |
| REG-191 | Parent-rule metadata shape drift across effective/raw/legacy fields | Runtime and backfill could diverge when reading mixed metadata documents (`type_rules` vs legacy `parent_rules`), leading to inconsistent parent-required/allowed-parent behavior during schema transition | Introduced nested `type_rules`/`type_rules_raw` as primary metadata contract with explicit `parent.allowed` + `parent.required` semantics, resolver read precedence (`type_rules` -> legacy -> fallback), compatibility persistence of legacy fields during transition, and regression coverage in `test_provider_parent_rules.py`, `test_jira_create_metadata_cache.py`, `test_work_item_parent_pending_handler.py` | Active |
| REG-192 | Provider metadata bloat and stale mirror drift | Legacy mirror/raw fields in `work_item_provider_metadata` (`parent_rules`, `parent_rules_raw`, `type_rules_raw`) could drift from canonical rules and increase maintenance/scan overhead | Lean-schema cutover: persist/read only canonical `allowed_create_types` + `type_rules`, unset legacy mirror/raw fields during metadata refresh, and keep provider fallback behavior for missing entries; covered by `test_provider_parent_rules.py` and `test_jira_create_metadata_cache.py` | Active |
| REG-193 | Parent-required gate drift due ingest/order dependency | Jira create-metadata ingest could discard provider parent semantics when transforming to lean schema, and schema-driven parent follow-up/validation could regress if activated before cache correctness | Convert Jira metadata `parent_rules` into canonical `type_rules` at ingest, then drive both follow-up trigger and create-time required-parent checks from resolved parent rules (with conservative subtask fallback only when metadata resolution is unavailable), plus remove stale pending payload parent-required flag; covered by parent-rule resolver/cache/constraint tests | Active |
| REG-194 | Pending parent-resume assignee drift + Jira partial-response format deviation | Parent-selection resume could overwrite first-turn assignee intent by re-extracting from free-text `for ...` phrases (for example title text), causing wrong assignee follow-up attempts; and create-success + assign-failure turns could return inconsistent narrative output instead of deterministic created-item structure | Restrict assignee hint extraction to explicit assignee phrases, persist and prefer first-turn assignee hint through pending context/resume payload, and normalize Jira create+assign partial outcomes to deterministic `Completed` + `Assignment pending` sections grounded in committed-actions/tool-errors; covered by pending-handler and orchestrator partial-response tests | Active |
| REG-195 | Parent-required clarification perceived latency gap in Slack DM | Parent-required create requests could stay silent while parent-candidate ranking runs, making Slack users think the bot stalled before options are shown | Jira create tool now sends a best-effort intermediate Slack progress message when parent is required and missing, then continues with standard candidate prompt; covered by `test_jira_subtask_parent_pending.py` (Slack-path send + non-Slack no-send guards) | Active |
| REG-196 | Parent suggestion quality drift + duplicate pending ranking overlap | Subtask parent suggestions could still over-surface broad parents (Epic) due weak context signals, and repeated equivalent create requests could trigger parallel ranking runs that return conflicting top-3 lists | Switched to enriched single-pass ranking context (policy confidence, preferred/deprioritized parent types, real recency tiers, similar-mapping context) and added equivalent-request pending reuse guard to avoid duplicate in-flight ranking; covered by pending-parent handler and Jira subtask pending tests | Active |
| REG-197 | Jira create success response missing parent line | Subtask creation confirmations could omit parent key in final Slack response even when tool execution had parent context (`under PRM-XXX`), causing inconsistent user-visible output | Added orchestrator post-processor to enforce `Parent` line for successful `jira_create_work_item` responses (`Parent: <KEY>` when available, otherwise `Parent: N/A`) with focused tests in `test_cortex_action_item_partial_response.py` | Active |
| REG-198 | Parent recommendation list noise and verbose clarification copy | Parent clarification could over-show low-relevance candidates (large fallback list) and use verbose progress/header copy that slowed user selection flow | Added confidence-threshold filtering before parent candidate pool assembly (`>=0.25`), constrained fallback to minimal top options, reduced default pool size, and replaced progress/header copy with concise relevant-option wording; covered by pending-parent handler and Jira subtask pending tests | Active |
| REG-199 | Subtask->Epic hierarchy invalid suggestion leak | Parent suggestion/metadata fallback could include Epic as a valid Sub-task parent, leading to Jira 400 hierarchy failures after user selection | Enforced Jira Sub-task parent invariant in shared parent-rule resolver/effective rules (Epic removed from fallback and metadata-effective allowed parents; Sub-task parent_required enforced), plus pending/create constraint regression coverage in `test_provider_parent_rules.py`, `test_work_item_parent_pending_handler.py`, and `test_jira_create_parent_constraints.py` | Active |
| REG-200 | Parent ranking ignored description context and overfit weak title-only matches | Parent recommendations for subtask clarification could rank unrelated tasks highly when title tokens were sparse/generic because ranking/similar-mapping context used mostly title-only signals | Enriched parent-ranking context with `child_description` and candidate `description`, updated similar-mapping overlap to use title+description text, and aligned ranking prompt contract/priorities to require title+description topical evidence before confidence scoring; covered by parent-handler regression suite (`test_work_item_parent_pending_handler.py`) | Active |
| REG-201 | Pending parent-resume blocked by write target grounding guard | After selecting a parent in pending subtask clarification (for example reply `1`), resumed create could fail with `target_scope_clarification_required` because selected parent key was not propagated into runtime grounding refs | Added `resume_target_refs` propagation across pending handler -> pending router -> Slack ingress metadata -> orchestrator feature context (`runtime_target_refs`) so non-idempotent grounding accepts trusted pending-selected parent identifiers; covered by pending handler/router tests and runtime grounding ref test | Active |
| REG-202 | Parent ranking fallback truncated candidate pool to top-3, breaking show-more/list-all | When ranking LLM timed out/failed parse (`llm_ranked=0`) or returned a short set, candidate pool persisted only top-3, so `show more`/`list all` could not surface additional eligible parents despite larger eligible set | Expanded post-rank pool fill to `_MAX_CANDIDATE_POOL` from eligible set (preserving ranked order first), aligned prompt input-key names with runtime payload fields, added ranking-timeout override setting (`work_item_parent_ranking_llm_timeout_seconds`), and added explicit ranking failure/parse diagnostics; covered by pending-parent handler regression tests | Active |
| REG-203 | Closed parent items surfaced in subtask parent suggestions | Parent analysis could recommend `done/closed` parents for new subtasks, which is usually undesirable for active planning even if Jira technically allows it | Parent suggestion eligibility now excludes closed-status parents before ranking/pool generation while preserving permissive explicit-key override for intentional user choices; ranking context is built from non-closed candidates for consistency, with focused pending-handler tests | Active |
| REG-204 | Optional-parent create guidance gap + missing explicit parent-link mutation tool | Optional-parent work-item creates could complete without any parent-mapping guidance, and users lacked a first-class mutation path to link existing work items under valid parents after create | Added non-blocking post-create best-practice recommendation for optional-parent creates, introduced `jira_update_parent_work_item` capability backed by shared update contract (`UpdateTaskDTO.parent_external_id`) and provider-rule hierarchy validation before write, with focused Jira tool/service/adapter tests (`test_jira_update_parent_tool.py`, updated task-service wiring tests) | Active |
| REG-205 | Typed read-filter ambiguity for story/epic/task queries | Read queries such as `list all epics` and `list all user stories` could return inconsistent or unfiltered results because type intent was not consistently parsed/applied across deterministic fastpath, Brain query path, and Jira live JQL path | Added provider-type intent extraction (`provider_types`) with singular/plural/synonym support, enforced provider-type filtering in both Brain and live Jira search paths, and added regression coverage in `test_cortex_task_lifecycle_policy.py` for typed and generic noun scenarios | Active |
| REG-206 | Internal tool identifier leakage in user-facing failure messages + parent-link mutate crash | Tool failures could expose internal tool IDs in user-visible messages (for example ``jira_search_work_items`` / ``jira_update_parent_work_item``), and Jira parent-link mutate could fail with adapter attribute error due stale helper call | Sanitized user-facing grounded tool-error fallback messages to remove internal tool identifiers, replaced tool-not-registered/not-implemented/not-wired user text with capability-safe phrasing, and fixed Jira adapter parent-link mutate path to use active `_validate_parent_constraints_for_create` helper; covered by `test_cortex_task_read_fastpath.py` and `test_jira_update_parent_tool.py` | Active |
| REG-207 | Analyze workload aggregation ambiguity and scope drift | Workload analysis could mis-scope specific-assignee requests when assignee value was a non-email hint, lacked type-filter parity with search (`provider_types`), and produced ambiguous reporting semantics where overdue counts looked additive instead of subset-of-open | Added `provider_types` support to `jira_analyze_work_items`, added specific-assignee non-email fallback (`name_exact` when email cannot be resolved), canonicalized status buckets for aggregate counts, and added explicit overdue-subset metadata + per-assignee overdue counts; covered by `test_jira_analyze_work_items.py` | Active |
| REG-208 | Cadence fetch scope ambiguity under mixed project collection entities | Cadence assigned-item fetch relied on status+assignee shape only and did not explicitly constrain `entity_type`, which risked future mixed-entity leakage as project collections evolve | Cadence fetch now enforces `entity_type="work_item"` and Cadence item cards include explicit work item type metadata (`provider_type`/`work_item_type`) in session payload + Slack formatting for clearer user-visible update context (`task_fetcher.py`, `session_store.py`, `formatters.py`, `checkin.py`) | Active |
| REG-209 | Planner charter cache stale stage/status after mutation | Planner read-path Redis cache could serve stale `status`/`entities`/`current-stage` when mutation routes missed deterministic invalidation wiring | Added planner read-cache invalidation hooks across stage-scoped and project-wide planner mutations (chat/extract/revision/draft/finalize/reset/docs/topic activation) with focused route/cache coverage (`test_planner_router_cache.py`, `test_planner_cache.py`, `test_pm_board_summary_charter_status_cache.py`) | Active |
| REG-210 | Charter init race + submenu latency/flicker | Charter page mounted with split async init (`charter-mode`/`topics`/`status`/`current-stage`/`documents`) causing default-stage flash (`goal` -> later stage), duplicate read fan-out, and slow submenu transitions when history checks blocked mode switching | Added server bootstrap contract (`GET /api/planner/{project_id}/bootstrap`) and single frontend bootstrap init flow, removed competing current-stage initializer, made stage switch non-blocking with async history refinement, and centralized document-list ownership in planner parent (`test_planner_router_cache.py`) | Active |
| REG-211 | Cadence timeout notice duplicate delivery under concurrent send paths | Timeout closure notice could be sent twice when expiry sweep path and DM-router fallback path attempted delivery concurrently (check-then-send race before `expiry_notice_sent_at` persisted) | Added atomic timeout notice claim (`expiry_notice_claimed_at`) before send, release-on-failure, and sent-marker finalize with claim cleanup so both caller paths share one idempotent send contract (`session_store.py`, `interaction_handler.py`) | Active |
| REG-212 | Charter bootstrap cold-load latency and repeated per-stage fan-out | Charter initial load could block on full multi-stage hydration and repeatedly refetch stage entities/doc state on first entry, causing ~10s cold starts and visible submenu delays/flicker | Added async-first charter bootstrap contract (`/bootstrap` returns `202 processing` + partial first-stage payload), status polling endpoint (`/bootstrap-status`), background bootstrap hydration job + Redis payload/status/lock keys, source-tagged charter read/write path with legacy fallback, and frontend progressive hydration with stale-safe poll loop (`test_planner_router_cache.py`, `test_planner_cache.py`, `test_planner_bootstrap_queue_runtime.py`) | Active |
| REG-213 | Charter finalized-state regression on navigation/remount | Charter UI could show `Finalize` CTA for already-finalized stages after page re-entry because partial bootstrap topic status depended on discussion presence and frontend treated partial bootstrap payloads as cache-ready state, overwriting finalized maps | Backend now computes planner status from entity documents (canonical) across `/bootstrap`, `/status`, and `/current-stage`; partial bootstrap payloads are marked `partial=true`; frontend applies partial payloads as non-destructive merges and only persists ready bootstrap cache snapshots | Active |
| REG-214 | Charter start-fresh/extract stale stage list after reset | After `Start Fresh` or fresh document extraction, UI could still display old stage entries due module-level planner caches (`bootstrapPayloadCache`, `stageEntityGlobalCache`) surviving state reset; Redis full invalidation also left bootstrap runtime status/lock keys behind | Frontend now clears project-scoped module caches and local stage cache refs on reset/extract before refresh; backend full planner invalidation now also clears bootstrap payload/status/lock keys to avoid stale runtime state carry-over | Active |
| REG-215 | Work-item list scope/type/status/icon inconsistency across Slack and dashboard | Generic reads like `list tasks` could over-filter to Jira `Task` only, short bug queries could trigger clarification, dashboard could show task icon for bug rows, and custom Jira statuses were collapsed/hidden in list displays and Slack updates | Updated task-read intent to keep generic nouns (`tasks/items/tickets/issues/requests/cards`) cross-type by default and include unassigned unless explicitly self-scoped, expanded scope entity hints for bug/defect/request nouns, preserved Jira raw status for display (`status_display`/`status_raw`) while retaining canonical status for analytics, and added explicit bug icon rendering paths in project health views; covered by updated parser/scope tests (`test_cortex_task_lifecycle_policy.py`, `test_domain_scope_gate.py`, `test_cortex_parsing_adapter.py`, `test_cortex_executor_parse_gate.py`, `test_cortex_orchestrator_write_preflight.py`) | Active |
| REG-216 | Work-item icon drift across provider/native types | Pulse Work Item rows could show inconsistent/generic icons because rendering favored legacy `issue_type` only, making bug/story/subtask/custom types appear random | Added shared Atlassian-like icon registry with canonical-first + provider-token fallback resolution across common issue types (`epic`, `story`, `task`, `bug`, `subtask`, `service`, etc.) and a single stable custom icon for unknown/new types; wired Pulse Work Item hierarchy rows/tooltips to this shared resolver for consistent cross-tool rendering (`work-item-icons.tsx`, `project-health-hierarchy.tsx`) | Active |
| REG-216 | Invalid parent input could trigger unintended parent selection/update in pending clarification flow | Parent clarification accepted broad key-shaped text and allowed LLM unclear-intent follow-up to resolve `select`, so incorrect replies could still advance parent mutation instead of forcing explicit valid parent confirmation | Added shared parent-reference validator (Brain-first with Jira-live fallback), enforced parent existence checks before resume/create/update-parent execution, and removed LLM auto-select behavior from unclear pending replies so only explicit number/key selections can proceed; covered by `test_work_item_parent_pending_handler.py`, `test_jira_update_parent_tool.py`, and `test_parent_reference_validator.py` | Active |
| REG-217 | Cadence closed-summary missed custom provider done statuses | Work items moved to custom done-equivalent statuses (for example Jira `QA TEST` mapped to done category) could update provider state but fail to appear under Cadence completed/closed summaries because completion detection required literal `done` | Reused shared status/category semantics in Cadence summary/session formatting: completion now uses project `recommendation_status_category_map` + canonical done-equivalent detection, with regression coverage in `test_cadence_status_completion_semantics.py` | Active |
| REG-218 | DM `switch project` discoverability and safe precedence gap | In multi-project DM flows, users could forget active context and natural-language `switch project` did not open the new picker flow; switching during pending clarification could create context confusion | Added strict DM `switch project` intercept after Cadence/Team Poll terminal routing, sends button-driven modal opener (`project_switch_open_modal`) that reuses existing switch modal contract, and blocks switch while non-terminal work-item/action-item clarifications are pending; covered by `test_slack_project_switch_modal.py` and `test_slack_router_response_guards.py` | Active |
| REG-219 | Missing active-project reminder on multi-project tool responses in DM | In multi-project DM conversations, tool-driven responses could omit active project context, increasing wrong-project follow-up risk after context drift | Added shared Slack context-footer helper and applied it to Cortex inline DM responses and pending clarification DM replies when membership spans multiple projects (`Current Project ... switch project`), with regression coverage in `test_slack_context_footer.py` | Active |
| REG-220 | Context footer shown on static Cortex responses | Multi-project DM footer could appear on non-tool/static Cortex replies, creating noisy reminders on generic responses instead of critical tool-driven outputs | Added explicit tool-call gating (`used_tool_call`) in Cortex handoff + router footer decision so footer is appended only for DM responses backed by at least one tool call; covered by `test_cortex_slack_handoff.py` and `test_slack_router_response_guards.py` | Active |
| REG-221 | Slack standardized work-item list showed normalized status tokens instead of provider raw labels | Read result compaction dropped `status_raw` / `status_display`, so Slack list rendering fell back to normalized `status` values (`in_progress`, `todo`) even when Jira had custom labels (for example `QA TEST`, `Backlog`) | Preserved `status_raw`/`status_display` in read compaction and standardized list rendering now prefers raw/display labels with explicit missing-label guard; covered by `test_cortex_executor_read_compaction.py` and `test_cortex_task_read_fastpath.py` | Active |
| REG-222 | Footer missing on normal Cortex tool-call DM deliveries | Footer injection was tied to router fallback `handoff.response_text`, while most successful tool-call replies are delivered directly from orchestrator, causing no footer on normal tool outputs | Reused shared footer helper inside orchestrator delivery path for Slack DM tool-call messages (`tool_calls_count > 0`) while keeping static/greeting paths excluded; covered by `test_cortex_orchestrator_footer_delivery.py` | Active |
| REG-223 | Jira webhook auth/model mismatch with REST-registered webhooks | Jira webhook verifier expected HMAC signature header (`X-Hub-Signature`) while webhook registration payload did not include signing secret, causing fail-closed `401/503` behavior depending on env/secret state | Jira webhook auth now uses per-project URL token verification via shared verifier dependency (`require_webhook_auth("jira")`) on project-scoped route (`/api/jira/webhooks/{project_id}/task-updated?token=...`) with verifier and queue/runtime regression coverage (`test_jira_webhook_verifier.py`, `test_jira_webhook_queue_runtime.py`, `test_jira_webhooks_recommendation_dirty.py`) | Active |
| REG-224 | Web production image build dependency conflict | `docker compose -f docker-compose.prod.yml up -d --build` failed at `web` image `npm ci --omit=dev` due `@atlaskit/icon` peer dependency on React 18 while web runtime is React 19 | Removed direct Atlaskit icon dependency/imports and centralized work-item icon rendering on React 19-safe local registry (`lucide-react` + inline SVG fallbacks) so production `npm ci` resolves without peer override flags | Active |
| REG-225 | Projects page false onboarding redirect on backend fetch failure | Users with existing projects could be redirected to `/project-setup` because `/projects` route treated non-OK backend responses as empty project list, and server-side fetch preferred public API URL over internal service URL | `/projects` server route now prefers `PYTHON_API_URL` (internal service path), redirects to `/login` only for `401/403`, and throws on other backend failures instead of coercing to empty projects; onboarding redirect is now reserved for true empty-project responses | Active |
| REG-226 | Slack post-connect channel modal auto-open missed | After Slack OAuth success, Projects page URL-sync cleanup removed `slack_connected`/`project_id` before callback handling consumed them, so channel setup modal did not auto-open and users had to click `Configure Channels` manually | Preserve Slack callback params during generic URL sync, and clear them only inside dedicated Slack callback handler after queuing modal open (`web/components/projects/projects-page.tsx`); validate callback roundtrip: connect Slack -> redirect -> channel modal opens automatically | Active |
| REG-227 | Dramatiq worker restart loop on ElastiCache Serverless script restriction | Worker consumers (`workitem_events`, `action_item_extraction`) crashed with `ResponseError: This Redis command is not allowed from script ... @user_script:128` because Dramatiq Redis dispatch maintenance path executes Lua command patterns incompatible with ElastiCache Serverless restrictions | Standardize production compose/env to local Docker Valkey broker (`redis://valkey:6379`) and reduce worker thread defaults for small-host stability (`DRAMATIQ_WORKER_THREADS=2`); validate: workers stay healthy and queue consumption proceeds without recurring Lua script failures | Active |
| REG-228 | Work-item lifecycle stale PM summary replay after cleanup/reconnect | Work-item cleanup/reconnect could still show stale PM Board cards because Redis/snapshot invalidation was incomplete and browser session cache could replay old summary payloads even when Brain `work_item` rows were fresh | Enforce full PM summary lifecycle invalidation after work-item cleanup/disconnect (clear Redis + snapshot, mark dirty, enqueue recompute), treat stale `processing` without lock as failed in summary-status, and clear PM browser session cache on integration cleanup/disconnect/connect-success paths | Active |
| REG-229 | Jira create-type mismatch caused non-recoverable generic failure | `jira_create_work_item` could fail with provider-specific type errors (for example Task not allowed, only Bug) but runtime surfaced only generic failure text and required user restart instead of inline recovery | Add create preflight for provider-type constraints before write, rank allowed types with LLM (deterministic fallback), emit structured `provider_type_clarification_required`, and reuse Cortex pending-question flow for text-only follow-up (`yes`/explicit type) to resume the same create call; covered by `test_jira_create_provider_type_preflight.py` and `test_cortex_provider_type_pending_question.py` | Active |
| REG-230 | Jira provider-type clarification follow-up not consumed (`yes` ignored) | Clarification prompt for invalid Jira issue type could be shown, but follow-up replies (`yes` / explicit type) were treated as fresh chat instead of resuming create flow due Redis-session `pending_question` coupling and routing-order mismatch | Migrate provider-type clarification to shared `pending_interactions` DM router path (`provider_type_clarification`) with deterministic resume/cancel handling, and keep `pending_question` for other flows only; covered by `test_pending_provider_type_handler.py` and `test_pending_interactions_router.py` | Active |
| REG-231 | Cadence DM burst load stalled webhook loop and allowed overlapping session turns | Cadence DM routing used in-process `asyncio.create_task` from webhook path, so concurrent LLM/integration turns could contend with ingress and race mutate the same session | Route Cadence DM through Dramatiq `cadence_dm` queue and enforce per-session Redis lease fencing in queued executor (`handle_cadence_dm_queued`); covered by queue-runtime + lease/metrics unit tests (`test_queue_runtime_selection.py`, `test_session_lease.py`, `test_queue_metrics.py`) | Active |
| REG-232 | Cadence queued worker token/client bootstrap drift | Cadence DM queued worker path attempted Slack client creation with double decrypt (blank `InvalidToken` failures) and could run without capability bundles installed in worker execution path, causing adapter-missing no-op mutations | Queued path now passes encrypted token directly to Slack client factory, cadence worker actors ensure built-in capability bundles are installed before processing (`cadence_dm` + modal submit), and cadence progression logs include committed index transitions for deterministic diagnosis; covered by `test_cadence_queued.py`, `test_dramatiq_actor_capability_bootstrap.py`, and `test_dramatiq_app_bootstrap.py` | Active |
| REG-233 | Cadence summary/count drift and queue payload serialization failures | Cadence final Slack summary could report stale completion counts when session refetch relied on `session_index` after daily-summary cleanup, and action-item extraction enqueue from cadence summary could fail when payload contained raw `datetime` values | Cadence summary path now refetches by explicit `project_id` and uses shared completion semantics fallback, while action-item extraction enqueue normalizes payloads to JSON-safe primitives (`datetime` -> ISO strings); covered by `test_action_items_extraction_queue.py` | Active |
| REG-234 | Cortex factual over-assertion from non-authoritative context | Free-text DM questions about live state (for example integration connected status) could be answered confidently from historical/contextual evidence (for example existing work items) instead of authoritative status fields/tools | Added global Cortex response-grounding policy in shared response rules and fallback prompt defaults: no assumption-as-fact, explicit uncertainty on partial evidence, and explicit prohibition on inferring live status from historical artifacts alone | Active |
| REG-235 | Field-specific work-item follow-ups collapsed into generic list output + rigid count wording | Follow-up read questions (`who created`, `description`, `link`) could be rendered as the same standardized list template, and count responses used rigid fixed phrasing that missed project-native status insights | Standardized work-item list override now applies only to explicit browse-style read intents (non-count), read compaction preserves bounded detail fields (`created_by`, `description`, `link`, timestamps), policy requires direct field answers or explicit missing-field response for detail prompts, and open-count responses now return dynamic status breakdown from project-native labels with natural wording and partial-result note; covered by `test_cortex_task_read_fastpath.py` and `test_cortex_executor_read_compaction.py` | Active |

## How To Use
1. Add a row for every production/local incident.
2. Link the test/spec or guardrail PR.
3. Do not close until automated or repeatable validation exists.
